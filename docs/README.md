<!-- code2docs:start --># resplit

![version](https://img.shields.io/badge/version-0.1.0-blue) ![python](https://img.shields.io/badge/python-%3E%3D3.11-blue) ![coverage](https://img.shields.io/badge/coverage-unknown-lightgrey) ![functions](https://img.shields.io/badge/functions-5549-green)
> **5549** functions | **0** classes | **84** files | CC̄ = 2.6

> Auto-generated project documentation from source code analysis.

**Author:** Tom Sapletta  
**License:** Apache-2.0[(LICENSE)](./LICENSE)  
**Repository:** [https://github.com/semcod/resplit](https://github.com/semcod/resplit)

## Installation

### From PyPI

```bash
pip install resplit
```

### From Source

```bash
git clone https://github.com/semcod/resplit
cd resplit
pip install -e .
```

### Optional Extras

```bash
pip install resplit[screenshots]    # screenshots features
pip install resplit[tui]    # tui features
pip install resplit[semantic]    # semantic features
pip install resplit[full]    # full features
pip install resplit[dev]    # development tools
```

## Quick Start

### CLI Usage

```bash
# Generate full documentation for your project
resplit ./my-project

# Only regenerate README
resplit ./my-project --readme-only

# Preview what would be generated (no file writes)
resplit ./my-project --dry-run

# Check documentation health
resplit check ./my-project

# Sync — regenerate only changed modules
resplit sync ./my-project
```

### Python API

```python
from resplit import generate_readme, generate_docs, Code2DocsConfig

# Quick: generate README
generate_readme("./my-project")

# Full: generate all documentation
config = Code2DocsConfig(project_name="mylib", verbose=True)
docs = generate_docs("./my-project", config=config)
```




## Architecture

```
resplit/
├── SUMR
├── goal
├── infra-map
├── Makefile
├── SUMD
├── PLAN
├── tree
├── TODO
├── CHANGELOG
├── project
    ├── case_study_c2004
    ├── usage
    ├── c2004
    ├── architecture
    ├── walk_dry_run
    ├── Makefile
    ├── restore_endpoint
        ├── run
        ├── mock_results
        ├── README
        ├── README
        ├── README
        ├── run
        ├── docker-compose
        ├── README
        ├── run
        ├── README
        ├── pipeline
        ├── README
                        ├── result
                ├── autoloop-state
                ├── topology
                        ├── schema
                            ├── toon
                            ├── toon
                            ├── toon
                            ├── toon
                            ├── toon
                            ├── toon
                            ├── toon
                    ├── testql-run-iter4
                    ├── pytest-iter6
                    ├── pytest-iter4
                    ├── stabilization-summary
                    ├── pytest-iter5
                    ├── iteration
                    ├── pytest-iter1
                    ├── testql-run-iter7
                    ├── topology
                    ├── llm-decision
                    ├── pytest-iter7
                    ├── topology-iter6
                    ├── testql-run-iter5
                    ├── testql-run-iter1
        ├── README
                        ├── main
                        ├── main
                        ├── main
                        ├── main
                    ├── config
                    ├── main
            ├── docker-compose
            ├── Dockerfile
        ├── toon
            ├── toon
            ├── toon
├── pyproject
    ├── prompt
        ├── toon
├── README
        ├── toon
    ├── context
        ├── toon
        ├── toon
    ├── README
    ├── README
    ├── calls
        ├── toon
```

## API Overview

### Functions

- `all()` — —
- `run_migration()` — —
- `main()` — —
- `set_default()` — —
- `add_to_api_main()` — —
- `add_to_backend_main()` — —
- `create_performance_demo()` — —
- `create_requirements()` — —
- `slow_function()` — —
- `async_slow_function()` — —
- `simulate_api_calls()` — —
- `simulate_queries()` — —
- `create_api_timing_middleware()` — —
- `setup_performance_monitoring()` — —
- `create_db_profiling_engine()` — —
- `profile_function()` — —
- `profile_async_function()` — —
- `get_database_url()` — —
- `run_migrations_offline()` — —
- `run_migrations_online()` — —
- `upgrade()` — —
- `downgrade()` — —
- `get_source_db_path()` — —
- `include_object()` — —
- `ensure_device()` — —
- `ensure_customer()` — —
- `ensure_workshop()` — —
- `seed_template_variables()` — —
- `parse_date()` — —
- `parse_xml_protocol()` — —
- `seed()` — —
- `build_scenario()` — —
- `gen_scenario_id()` — —
- `upsert_scenario()` — —
- `seed_scenarios_list()` — —
- `run_seeds()` — —
- `list_seeds()` — —
- `mem_engine()` — —
- `generate_js_definition()` — —
- `safe_drop_table()` — —
- `noop_downgrade()` — —
- `safe_drop_column()` — —
- `table_exists()` — —
- `has_column()` — —
- `ensure_table()` — —
- `run_safely()` — —
- `resolve_dv_xml_path()` — —
- `read_vars_from_xml()` — —
- `insert_if_missing()` — —
- `safe_query()` — —
- `safe_query_scalar()` — —
- `load_legacy_table()` — —
- `create_table_if_missing()` — —
- `normalize_string()` — —
- `early_return_if_table_missing()` — —
- `get_db()` — —
- `get_config_db()` — —
- `get_menu_db()` — —
- `get_command_bus()` — —
- `get_query_bus()` — —
- `get_event_store()` — —
- `add_api_middlewares()` — —
- `get_openapi_config()` — —
- `api_response()` — —
- `is_valid_slug()` — —
- `is_valid_uuid()` — —
- `is_valid_email()` — —
- `slugify()` — —
- `validate_menu_name()` — —
- `validate_item_id()` — —
- `validate_route()` — —
- `create_app()` — —
- `create_api_router()` — —
- `ensure_cqrs_ready()` — —
- `get_session_for_table()` — —
- `check_memory_cache()` — —
- `check_database_connection()` — —
- `check_system_resources()` — —
- `check_environment()` — —
- `check_external_services()` — —
- `get_full_diagnostics()` — —
- `generate_recommendations()` — —
- `get_quick_diagnostics()` — —
- `start_protocol()` — —
- `get_protocol()` — —
- `get_protocol_document()` — —
- `execute_step()` — —
- `update_data()` — —
- `regenerate_document()` — —
- `pause_protocol()` — —
- `resume_protocol()` — —
- `complete_protocol()` — —
- `websocket_endpoint()` — —
- `sitemap_xml()` — —
- `robots_txt()` — —
- `list_users()` — —
- `select_user()` — —
- `login()` — —
- `get_session()` — —
- `logout()` — —
- `cql_proxy_info()` — —
- `cql_proxy_health()` — —
- `cql_quote()` — —
- `cql_unquote()` — —
- `cql_format_literal()` — —
- `cql_canonicalize()` — —
- `cql_normalize()` — —
- `cql_highlight()` — —
- `cql_parse()` — —
- `cql_serialize()` — —
- `cql_validate()` — —
- `cql_exec()` — —
- `cql_scenario_build()` — —
- `cql_capabilities()` — —
- `get_model()` — —
- `create_sgi()` — —
- `list_all_tables()` — —
- `list_rows()` — —
- `get_table_schema_alt()` — —
- `create_row()` — —
- `get_row()` — —
- `update_row()` — —
- `delete_row()` — —
- `parse_datetime()` — —
- `coerce_bool()` — —
- `coerce_column_types()` — —
- `remap_reserved_columns()` — —
- `normalize_datetime_fields()` — —
- `coerce_sgi_enabled()` — —
- `try_sgi_upsert()` — —
- `enrich_device_from_type()` — —
- `normalize_dsl_protocol_variables()` — —
- `normalize_template_variables()` — —
- `insert_with_retry()` — —
- `insert_template_variable()` — —
- `row_to_dict()` — —
- `parse_json_filters()` — —
- `apply_eq_filters()` — —
- `text_search()` — —
- `safe_execute_list_query()` — —
- `build_table_schema()` — —
- `get_all_schemas()` — —
- `get_table_schema()` — —
- `clear_schema_cache()` — —
- `template_proxy_get()` — —
- `template_proxy_post()` — —
- `template_proxy_patch()` — —
- `template_proxy_delete()` — —
- `list_activities()` — —
- `get_activity()` — —
- `create_activity()` — —
- `update_activity()` — —
- `delete_activity()` — —
- `list_assignments()` — —
- `get_assignment()` — —
- `create_assignment()` — —
- `update_assignment()` — —
- `delete_assignment()` — —
- `get_user()` — —
- `demo_users()` — —
- `demo_login()` — —
- `get_backups()` — —
- `get_backup_status()` — —
- `get_backup_health()` — —
- `create_backup_now()` — —
- `restore_from_backup()` — —
- `restore_latest_all()` — —
- `get_databases()` — —
- `get_namespace()` — —
- `get_key()` — —
- `set_key()` — —
- `delete_key()` — —
- `delete_namespace()` — —
- `batch_read()` — —
- `batch_ops()` — —
- `register_state_ws_handlers()` — —
- `list_config_sections()` — —
- `create_planfile_auto_task()` — —
- `get_config_section()` — —
- `update_config_section()` — —
- `delete_config_section()` — —
- `reset_config_section()` — —
- `get_system_config()` — —
- `get_hardware_status()` — —
- `validate_config()` — —
- `test_network_connection()` — —
- `ensure_main_nav_item()` — —
- `bridge_sidebar()` — —
- `replace_config()` — —
- `ensure_module_menu()` — —
- `ensure_config()` — —
- `get_config()` — —
- `upsert_item()` — —
- `get_or_404()` — —
- `delete_or_404()` — —
- `list_table()` — —
- `raise_on_failure()` — —
- `get_or_create()` — —
- `assert_not_exists()` — —
- `create_customer()` — —
- `get_customer()` — —
- `list_customers()` — —
- `update_customer()` — —
- `list_data_tables()` — —
- `list_configurations()` — —
- `proxy_data_requests()` — —
- `list_devices()` — —
- `get_device()` — —
- `create_device()` — —
- `update_device()` — —
- `delete_device()` — —
- `create_test_scenario()` — —
- `update_test_scenario()` — —
- `delete_test_scenario()` — —
- `list_workshops()` — —
- `list_protocols()` — —
- `list_table_rows()` — —
- `get_table_row()` — —
- `create_table_row()` — —
- `update_table_row()` — —
- `bulk_replace_table()` — —
- `delete_table_row()` — —
- `search_devices()` — —
- `retire_device()` — —
- `assign_device_to_customer()` — —
- `list_scripts()` — —
- `run_script()` — —
- `list_runs()` — —
- `get_run()` — —
- `get_run_logs()` — —
- `dsl_scan()` — —
- `dsl_standardize()` — —
- `dsl_sync_tables()` — —
- `dsl_import_xml()` — —
- `dsl_import_xml_by_name()` — —
- `dsl_verify_xml_import()` — —
- `list_objects()` — —
- `get_object()` — —
- `list_functions()` — —
- `get_function()` — —
- `list_params()` — —
- `get_param()` — —
- `list_units()` — —
- `get_dsl_logs()` — —
- `export_dsl_logs()` — —
- `clear_dsl_logs()` — —
- `execute_dsl_script()` — —
- `validate_def()` — —
- `validate_def_syntax()` — —
- `build_dsl_log_filters()` — —
- `get_dsl_logs_payload()` — —
- `export_dsl_logs_payload()` — —
- `clear_dsl_logs_payload()` — —
- `save_dynamic_page()` — —
- `get_dynamic_pages()` — —
- `delete_dynamic_page()` — —
- `list_events()` — —
- `list_streams()` — —
- `append_events()` — —
- `clear_stream()` — —
- `list_flags()` — —
- `list_flags_admin()` — —
- `list_categories()` — —
- `create_flag()` — —
- `update_flag()` — —
- `update_flag_by_key()` — —
- `delete_flag()` — —
- `reset_flags()` — —
- `list_icons()` — —
- `get_icon()` — —
- `get_icon_by_name()` — —
- `create_icon()` — —
- `update_icon()` — —
- `delete_icon()` — —
- `list_identification_endpoints()` — —
- `identify()` — —
- `get_history()` — —
- `clear_history()` — —
- `get_health()` — —
- `identify_shortcut()` — —
- `update_config()` — —
- `get_env()` — —
- `get_status()` — —
- `heartbeat()` — —
- `fleet_status()` — —
- `trigger_sync()` — —
- `get_kiosk_logs()` — —
- `restart_kiosk()` — —
- `list_backups()` — —
- `create_backup()` — —
- `get_library()` — —
- `update_library()` — —
- `add_library_item()` — —
- `delete_library_item()` — —
- `get_logs()` — —
- `get_log_stats()` — —
- `get_error_logs()` — —
- `ingest_frontend_logs()` — —
- `clear_logs()` — —
- `list_allowed_names()` — —
- `create_configuration()` — —
- `get_configuration()` — —
- `get_configuration_by_name()` — —
- `update_configuration()` — —
- `delete_configuration()` — —
- `create_or_update_item()` — —
- `delete_item()` — —
- `capture_screenshots()` — —
- `get_configuration_by_route()` — —
- `get_sidebar_structure()` — —
- `list_modules()` — —
- `broadcast_update()` — —
- `resolve_system_chrome()` — —
- `take_screenshots_playwright()` — —
- `take_screenshots_cli()` — —
- `save_screenshot()` — —
- `get_screenshots()` — —
- `get_screenshot_file()` — —
- `delete_screenshot()` — —
- `clear_screenshots()` — —
- `list_modules_admin()` — —
- `create_module()` — —
- `update_module()` — —
- `delete_module()` — —
- `reset_modules()` — —
- `get_navigation_options()` — —
- `list_navigation_contexts()` — —
- `list_all_navigation_options()` — —
- `create_navigation_option()` — —
- `update_navigation_option()` — —
- `delete_navigation_option()` — —
- `reset_navigation_options()` — —
- `list_redirects()` — —
- `list_aliases()` — —
- `get_redirect()` — —
- `create_redirect()` — —
- `update_redirect()` — —
- `delete_redirect()` — —
- `get_redirect_by_route()` — —
- `generate_report()` — —
- `list_reports()` — —
- `get_report()` — —
- `export_report()` — —
- `delete_report()` — —
- `list_roles()` — —
- `get_roles_meta()` — —
- `update_role_meta()` — —
- `create_role()` — —
- `update_role()` — —
- `delete_role()` — —
- `get_menu_access()` — —
- `set_menu_access()` — —
- `bulk_update_menu_access()` — —
- `seed_missing_access()` — —
- `delete_menu_access()` — —
- `ensure_default_roles()` — —
- `ensure_default_access()` — —
- `get_scenario_activities()` — —
- `add_activity_to_scenario()` — —
- `remove_activity_from_scenario()` — —
- `create_scenario()` — —
- `get_fallback_scenario()` — —
- `get_service_scenario_health()` — —
- `get_scenario()` — —
- `list_scenarios()` — —
- `update_scenario()` — —
- `delete_scenario()` — —
- `get_scenario_dsl()` — —
- `execute_scenario()` — —
- `get_execution_status()` — —
- `get_execution_logs()` — —
- `stop_execution()` — —
- `list_tables_v3()` — —
- `list_schema_labels_v3()` — —
- `get_schema_root_v3()` — —
- `get_table_schema_v3()` — —
- `refresh_schema_cache_v3()` — —
- `get_schema_version_v3()` — —
- `get_service_data_health()` — —
- `get_service_manager_health()` — —
- `get_service_reports_health()` — —
- `get_service_workshop_health()` — —
- `scan_frontend_modules()` — —
- `scan_database_tables()` — —
- `compare_connect_data_menu()` — —
- `add_missing_tables_to_menu()` — —
- `hide_extra_menu_items()` — —
- `get_test_kinds()` — —
- `update_test_kinds()` — —
- `add_test_kind()` — —
- `delete_test_kind()` — —
- `create_test_session()` — —
- `get_test_session()` — —
- `list_test_sessions()` — —
- `start_test_session()` — —
- `complete_test_session_step()` — —
- `get_test_session_history()` — —
- `pause_test_session()` — —
- `resume_test_session()` — —
- `cancel_test_session()` — —
- `complete_test_session()` — —
- `delete_by_id()` — —
- `reset_table()` — —
- `compare_versions()` — —
- `events_websocket()` — —
- `handle_cqrs_command()` — —
- `handle_cqrs_query()` — —
- `setup_websocket_handlers()` — —
- `get_connected_clients()` — —
- `get_websocket_stats()` — —
- `broadcast_event()` — —
- `event_record_to_dict()` — —
- `pytest_configure()` — —
- `api_url()` — —
- `api_client()` — —
- `utcnow()` — —
- `api_available()` — —
- `get_demo_status()` — —
- `seed_demo_data()` — —
- `reset_demo_data()` — —
- `get_demo_config()` — —
- `save_demo_config()` — —
- `detect_devices()` — —
- `configure_connect_template_backend_client()` — —
- `close_connect_template_backend_client()` — —
- `get_connect_template_backend_base_url()` — —
- `health()` — —
- `list_drafts()` — —
- `save_draft()` — —
- `publish_template()` — —
- `proxy()` — —
- `configure_cql_backend_client()` — —
- `close_cql_backend_client()` — —
- `get_cql_backend_base_url()` — —
- `capabilities()` — —
- `quote()` — —
- `format_literal()` — —
- `unquote()` — —
- `canonicalize()` — —
- `normalize()` — —
- `highlight()` — —
- `init_config_db()` — —
- `discover_domain_handlers()` — —
- `auto_register_domain()` — —
- `register_event_types_from_package()` — —
- `register_events_handlers()` — —
- `register_live_protocol_handlers()` — —
- `register_menu_handlers()` — —
- `register_sensors_handlers()` — —
- `register_identification_handlers()` — —
- `setup_cqrs_infrastructure()` — —
- `get_cqrs_container()` — —
- `handle_load_sgi()` — —
- `handle_save_sgi()` — —
- `handle_delete_sgi()` — —
- `handle_bulk_save_sgi()` — —
- `register_sgi_handlers()` — —
- `register_sgi_bus_handlers()` — —
- `register_testing_handlers()` — —
- `create_crud_router()` — —
- `get_scenario_db()` — —
- `get_auth_db()` — —
- `init_db()` — —
- `init_auth_db()` — —
- `init_scenario_db()` — —
- `get_event_bus()` — —
- `init_devtools_db()` — —
- `lifespan()` — —
- `query_search()` — —
- `root()` — —
- `init_menu_db()` — —
- `copy_old_menu_data_if_present()` — —
- `ensure_columns()` — —
- `ensure_main_navigation_structure()` — —
- `ensure_default_route_redirects()` — —
- `seed_module_menus()` — —
- `seed_menu_items_from_registry()` — —
- `get_first_column()` — —
- `update_existing_item()` — —
- `get_correlation_id()` — —
- `get_trace_context()` — —
- `setup_structured_logging()` — —
- `setup_nfo()` — —
- `apply_module_bindings()` — —
- `get_last_backup_time()` — —
- `needs_backup()` — —
- `cleanup_old_backups()` — —
- `run_backup()` — —
- `restore_backup()` — —
- `ensure_backup_dir()` — —
- `check_last_backup()` — —
- `pg_dump()` — —
- `pg_restore()` — —
- `export_tables()` — —
- `normalize_object_name()` — —
- `goals_from_cql()` — —
- `load_cql()` — —
- `scan_scenario_dsl_coverage()` — —
- `ensure_dsl_tables_updated()` — —
- `create_scenario_with_dsl()` — —
- `extract_from_dsl()` — —
- `extract_from_goals()` — —
- `parse_goals_from_dsl()` — —
- `dsl_from_goals()` — —
- `standardize_scenario_dsl_goals()` — —
- `sync_dsl_tables_from_scenarios()` — —
- `import_xml_file()` — —
- `import_xml_files()` — —
- `import_xml_by_name()` — —
- `verify_xml_import()` — —
- `get_repo_root()` — —
- `is_safe_xml_name()` — —
- `find_xml_files_by_name()` — —
- `normalize_text()` — —
- `read_xml_vars()` — —
- `generate_split_scenarios_from_vars()` — —
- `generate_dsl_from_xml_vars()` — —
- `goals_from_dsl_text()` — —
- `ensure_dsl_dictionary_tables()` — —
- `get_firmware_gateway()` — —
- `get_search_index_service()` — —
- `reset_search_index_service()` — —
- `gen_id()` — —
- `list_variables()` — —
- `get_schema()` — —
- `create_tables()` — —
- `create_client()` — —
- `test_placeholder()` — —
- `test_import()` — —
- `index_page()` — —
- `health_check()` — —
- `status()` — —
- `get_io_ports()` — —
- `test_probe_waveshare_modbus_detects_working_port()` — —
- `test_probe_waveshare_modbus_reports_adapter_only_when_no_response()` — —
- `test_extract_scenario_id_accepts_frontend_and_cli_keys()` — —
- `test_extract_inline_dsl_accepts_content_and_direct_fields()` — —
- `normalize_dsl_to_library()` — —
- `convert_goals_json_to_library()` — —
- `process_scenario_row()` — —
- `migrate_scenarios()` — —
- `process_scenario()` — —
- `verify_migration()` — —
- `nested_to_flat_route()` — —
- `normalize_column_id()` — —
- `get_module_from_route()` — —
- `seed_default_menu_structure()` — —
- `upsert_dsl_objects()` — —
- `upsert_dsl_functions()` — —
- `upsert_dsl_units()` — —
- `upsert_dsl_params()` — —
- `ensure_tables_and_seed()` — —
- `extract_entities_from_xml()` — —
- `build_scenario_content()` — —
- `link_object_functions_from_steps()` — —
- `upsert_scenario_row()` — —
- `nice_obj()` — —
- `get_db_path()` — —
- `load_dsl_data()` — —
- `migrate_dsl_to_def()` — —
- `rollback_migration()` — —
- `migrate_database()` — —
- `create_pg_schemas()` — —
- `backup_databases()` — —
- `check_table_exists()` — —
- `get_row_count()` — —
- `ensure_assignment()` — —
- `print_json()` — —
- `get_recent_scenarios()` — —
- `get_dsl_table_stats()` — —
- `check_table_existence()` — —
- `get_icons_from_db()` — —
- `migrate_file()` — —
- `migrate_frontend()` — —
- `generate_icon_mapping_file()` — —
- `parse_svg_definitions()` — —
- `categorize_icon()` — —
- `migrate_icons()` — —
- `load_icon_mapping()` — —
- `save_icon_mapping()` — —
- `is_unicode_icon()` — —
- `find_svg_mapping()` — —
- `list_menu_icons()` — —
- `preview_migration()` — —
- `apply_migration()` — —
- `list_current_mappings()` — —
- `edit_mappings()` — —
- `seed_encoder_config()` — —
- `get_target_column()` — —
- `validate_menu()` — —
- `fix_menu_columns()` — —
- `generate_def_code()` — —
- `get_logger()` — —
- `analyze_file()` — —
- `analyze_directory()` — —
- `test_setup_uses_module_registration_hooks()` — —
- `test_setup_is_idempotent_when_handlers_are_present()` — —
- `test_setup_continues_when_optional_module_registration_fails()` — —
- `client()` — —
- `event_loop()` — —
- `sample_identification_data()` — —
- `sample_device_data()` — —
- `spec()` — —
- `gateway()` — —
- `test_health_check()` — —
- `test_get_scenarios()` — —
- `test_get_scenario_by_id()` — —
- `test_get_scenario_not_found()` — —
- `test_register_dsl()` — —
- `test_start_execution()` — —
- `test_execute_step()` — —
- `test_send_command_start()` — —
- `test_send_command_unknown()` — —
- `test_get_projection()` — —
- `test_get_status()` — —
- `test_get_state()` — —
- `test_pause()` — —
- `test_resume()` — —
- `test_stop()` — —
- `test_retry_on_connect_error()` — —
- `test_spec_file_valid()` — —
- `service_data_contracts()` — —
- `test_service_data_slice_has_minimum_contracts()` — —
- `test_service_data_covers_both_modules()` — —
- `test_service_data_layers_python_exists()` — —
- `test_service_data_layers_frontend_exists()` — —
- `test_service_data_commands_have_transport()` — —
- `test_service_data_queries_have_transport()` — —
- `test_service_data_events_have_producers()` — —
- `test_service_data_filenames_match_content()` — —
- `health_client()` — —
- `test_health_endpoint_returns_200()` — —
- `test_health_endpoint_response_shape()` — —
- `test_health_endpoint_covers_slice_modules()` — —
- `test_health_endpoint_overall_reflects_worst_case()` — —
- `service_id_contracts()` — —
- `test_service_id_slice_has_minimum_contracts()` — —
- `test_service_id_covers_module()` — —
- `test_service_id_layers_python_exists()` — —
- `test_service_id_layers_frontend_exists_or_null()` — —
- `test_service_id_commands_have_transport()` — —
- `test_service_id_queries_have_transport()` — —
- `test_service_id_events_have_producers()` — —
- `test_service_id_filenames_match_content()` — —
- `test_service_id_health_returns_200()` — —
- `test_service_id_health_response_shape()` — —
- `test_service_id_health_checks_structure()` — —
- `test_service_id_health_aggregate_reflects_worst_case()` — —
- `manager_contracts()` — —
- `test_manager_slice_has_minimum_contracts()` — —
- `test_manager_contracts_have_discriminator()` — —
- `test_manager_contracts_have_kind()` — —
- `test_manager_contracts_filename_matches_discriminator()` — —
- `test_manager_contracts_python_layer_exists()` — —
- `test_manager_health_contract_exists()` — —
- `test_service_manager_health_returns_200()` — —
- `test_service_manager_health_response_shape()` — —
- `test_service_manager_health_checks_structure()` — —
- `test_service_manager_health_aggregate_reflects_worst_case()` — —
- `reports_contracts()` — —
- `test_reports_slice_has_minimum_contracts()` — —
- `test_reports_contracts_have_discriminator()` — —
- `test_reports_contracts_have_kind()` — —
- `test_reports_contracts_filename_matches_discriminator()` — —
- `test_reports_contracts_python_layer_exists()` — —
- `test_reports_health_contract_exists()` — —
- `test_service_reports_health_returns_200()` — —
- `test_service_reports_health_response_shape()` — —
- `test_service_reports_health_checks_structure()` — —
- `test_service_reports_health_aggregate_reflects_worst_case()` — —
- `workshop_contracts()` — —
- `test_workshop_slice_has_minimum_contracts()` — —
- `test_workshop_contracts_have_discriminator()` — —
- `test_workshop_contracts_have_kind()` — —
- `test_workshop_contracts_filename_matches_discriminator()` — —
- `test_workshop_contracts_python_layer_exists()` — —
- `test_workshop_health_contract_exists()` — —
- `test_service_workshop_health_returns_200()` — —
- `test_service_workshop_health_response_shape()` — —
- `test_service_workshop_health_checks_structure()` — —
- `test_service_workshop_health_aggregate_reflects_worst_case()` — —
- `mock_db_session()` — —
- `test_v3_protocols_accept_large_limit_over_http()` — —
- `cqrs_container()` — —
- `event_store_with_mock_db()` — —
- `test_devtools_dsl_routes_delegate_to_shared_services()` — —
- `test_dsl_execute_endpoint_returns_deprecation_payload()` — —
- `test_validate_def_endpoint_returns_valid_for_correct_code()` — —
- `test_validate_def_endpoint_returns_errors_for_invalid_code()` — —
- `test_validate_def_endpoint_returns_errors_for_empty_code()` — —
- `test_dsl_logs_export_has_canonical_logs_route_and_dsl_alias()` — —
- `test_health_check_quiet()` — —
- `test_execution_lifecycle()` — —
- `test_send_command()` — —
- `firmware_sim_server()` — —
- `test_start_live_protocol_dispatches_to_real_firmware_sim()` — —
- `test_live_protocol_api_flow_reuses_firmware_execution_id()` — —
- `test_live_protocol_api_lifecycle_delegates_to_firmware()` — —
- `fresh_container()` — —
- `test_menu_handlers_are_registered_in_buses()` — —
- `test_menu_commands_and_queries_dispatch_through_bus()` — —
- `test_scenario_endpoints_bridge_legacy_columns_from_storage()` — —
- `test_create_and_update_scenario_keep_library_and_legacy_columns_in_sync()` — —
- `test_sgi_handlers_are_registered_in_buses()` — —
- `test_sgi_commands_and_query_dispatch_through_bus()` — —
- `test_customer_devices_projection_groups_by_customer()` — —
- `test_customer_devices_projection_empty_list()` — —
- `test_device_list_projection_builds_latest_state()` — —
- `test_device_list_projection_handles_retired_device()` — —
- `test_device_list_projection_empty_events()` — —
- `ensure_user()` — —
- `test_session_no_user_returns_none()` — —
- `test_session_missing_id_is_recovered_and_persisted()` — —
- `test_invalid_session_without_username_is_cleared()` — —
- `test_id_is_always_string()` — —
- `test_backups_list_delegates_to_service()` — —
- `test_databases_endpoint_returns_configured_pg_labels()` — —
- `test_create_backup_returns_service_count()` — —
- `test_restore_backup_maps_service_errors()` — —
- `test_backup_health_endpoint()` — —
- `test_backup_status_pg_mode()` — —
- `test_restore_latest_endpoint_dry_run()` — —
- `setup_module()` — —
- `test_v3_config_settings_roundtrip()` — —
- `test_v3_config_reset_defaults()` — —
- `test_v3_config_hardware_status_alias()` — —
- `test_v3_protocols_accept_large_limit()` — —
- `test_v3_data_protocols_prefers_proxy_list()` — —
- `test_v3_data_protocols_falls_back_list_on_proxy_502()` — —
- `test_v3_data_protocols_prefers_proxy_get()` — —
- `test_v3_data_protocols_falls_back_get_on_proxy_502()` — —
- `test_v3_data_customers_create_and_patch_use_domain_surface()` — —
- `test_v3_data_devices_create_patch_and_delete_use_domain_surface()` — —
- `test_v3_data_test_scenarios_legacy_surface_uses_scenario_owner()` — —
- `test_v3_data_test_scenarios_prefers_proxy_path()` — —
- `test_v3_data_test_scenarios_falls_back_to_legacy_on_proxy_502()` — —
- `test_v3_data_sgi_create_and_load_for_type_device_context()` — —
- `test_v3_data_sgi_update_existing_type_row_via_patch()` — —
- `test_v3_menu_list_and_main_navigation()` — —
- `test_v3_schema_and_labels_do_not_500()` — —
- `test_v1_auth_session_does_not_500()` — —
- `test_menu_v2_v3_endpoints_do_not_500()` — —
- `backend_root()` — —
- `test_cqrs_script_exists()` — —
- `test_generated_menu_contracts_import_and_instantiation()` — —
- `test_append_frontend_logs_persists_browser_entries()` — —
- `teardown_module()` — —
- `test_upsert_main_navigation_connect_manager()` — —
- `test_create_connect_manager_config_and_by_route()` — —
- `test_get_menu_by_route_exact_match_flag_controls_prefix_resolution()` — —
- `test_module_menu_alias_resolves_to_base_configuration()` — —
- `test_create_configuration_persists_inactive_state()` — —
- `test_create_configuration_persists_item_active_and_visible_flags()` — —
- `test_update_configuration_persists_route_mappings()` — —
- `test_query_handler_resolves_menu_alias_without_warning()` — —
- `test_logging_after_operations()` — —
- `test_update_main_navigation_logs()` — —
- `db_session()` — —
- `clean_menu_config()` — —
- `test_update_configuration_passes_route_mappings_to_command()` — —
- `test_update_handler_applies_route_mappings()` — —
- `test_ensure_default_roles_skips_access_seed_by_default()` — —
- `test_ensure_default_roles_can_seed_access_when_requested()` — —
- `isolated_search_db()` — —
- `test_queries_search_returns_local_index_results()` — —
- `test_queries_search_returns_empty_when_service_unavailable()` — —
- `test_backend_version_endpoints_expose_version()` — —
- `test_version_compare_reports_match()` — —
- `test_version_compare_reports_mismatch()` — —
- `test_send_to_client_treats_normal_close_as_non_error()` — —
- `test_run_seeds_accepts_canonical_seed_history_schema()` — —
- `test_ensure_seed_history_migrates_legacy_runtime_schema()` — —
- `test_goals_from_cql_uses_oqlos_ast_path_for_basic_steps()` — —
- `test_goals_from_cql_falls_back_to_legacy_when_parse_cql_raises()` — —
- `test_data_list_customers_proxies_to_connect_data()` — —
- `test_data_get_device_proxies_to_connect_data()` — —
- `test_data_create_customer_proxies_to_connect_data()` — —
- `test_data_delete_device_proxies_to_connect_data()` — —
- `test_data_list_protocols_proxies_to_connect_protocol()` — —
- `test_data_list_protocols_filter_id_uses_get_details()` — —
- `test_data_get_protocol_maps_upstream_not_found()` — —
- `test_export_dsl_emits_replayable_api_lines_and_comments_unknown_entries()` — —
- `test_scan_scenario_dsl_coverage_counts_missing_and_invalid_rows()` — —
- `test_standardize_scenario_dsl_goals_updates_missing_fields()` — —
- `test_standardize_scenario_dsl_goals_builds_goals_from_real_task_style_dsl()` — —
- `test_sync_dsl_tables_from_scenarios_returns_counts_from_collected_entities()` — —
- `test_sync_dsl_tables_from_scenarios_bootstraps_missing_dictionary_tables()` — —
- `test_generate_dsl_from_xml_vars_emits_goal_tasks_and_conditions()` — —
- `test_generate_dsl_from_xml_vars_emits_wait_and_operator_confirmation()` — —
- `test_generate_dsl_from_xml_vars_maps_outputs_to_set_without_task_keyword()` — —
- `test_generate_dsl_from_xml_vars_maps_compressor_to_set_with_lpm()` — —
- `test_generate_split_scenarios_from_vars_splits_per_transaction()` — —
- `test_create_scenario_with_dsl_supports_legacy_schema_without_duration()` — —
- `test_import_xml_file_returns_single_file_import_details()` — —
- `test_start_handler_calls_firmware_and_persists_execution_id()` — —
- `test_execute_step_uses_firmware_execution_id_from_protocol()` — —
- `test_firmware_gateway_retries_connect_errors_then_succeeds()` — —
- `test_firmware_health_check_is_quiet_for_unreachable_service()` — —
- `test_firmware_gateway_get_scenario_uses_detail_endpoint()` — —
- `test_firmware_gateway_register_dsl_uses_scenarios_endpoint()` — —
- `test_get_firmware_gateway_uses_settings_base_url()` — —
- `test_protocol_lifecycle_handlers_delegate_to_firmware()` — —
- `test_ensure_success_invalid_json_returns_bad_gateway()` — —
- `test_ensure_success_not_found_domain_error()` — —
- `test_ensure_success_domain_error_maps_to_bad_request()` — —
- `test_start_protocol_forwards_payload()` — —
- `test_get_protocol_document_forwards_format_query()` — —
- `test_list_protocols_forwards_filters_and_pagination()` — —
- `test_pause_protocol_forwards_reason()` — —
- `test_resume_protocol_forwards_endpoint()` — —
- `test_complete_protocol_forwards_payload()` — —
- `test_run_column_migrations_adds_legacy_scenario_columns_even_for_partial_db()` — —
- `test_scenario_session_uses_main_engine_when_urls_match()` — —
- `test_goals_helpers_accept_legacy_goals_column_payload()` — —
- `test_effective_library_bridges_legacy_content_goals_and_def()` — —
- `test_normalize_sqlite_url_resolves_backend_relative_paths()` — —
- `test_raise_for_upstream_maps_not_found_domain_error()` — —
- `test_raise_for_upstream_invalid_json_returns_bad_gateway()` — —
- `test_delete_scenario_forwards_reason_to_connect_backend()` — —
- `test_delete_scenario_without_body_sends_null_json()` — —
- `test_get_scenario_dsl_maps_upstream_payload()` — —
- `test_execute_scenario_forwards_mode_and_speed()` — —
- `test_get_execution_logs_forwards_limit_param()` — —
- `test_get_fallback_scenario_returns_default_for_unknown_device()` — —
- `test_get_service_scenario_health_aggregates_upstream_checks()` — —
- `test_map_proxy_error_status_not_found()` — —
- `test_map_proxy_error_status_conflict_states()` — —
- `test_map_proxy_error_status_default_bad_request()` — —
- `test_ensure_success_raises_for_invalid_json()` — —
- `test_ensure_success_maps_domain_error()` — —
- `test_ensure_success_returns_payload()` — —
- `test_complete_test_session_step_proxies_and_maps_response()` — —
- `test_complete_test_session_step_raises_not_found()` — —
- `setup_test_databases()` — —
- `data_health()` — —
- `test_health_endpoint_reports_service_metadata()` — —
- `test_customers_endpoint_envelope()` — —
- `test_create_device_returns_legacy_data_shape()` — —
- `encoder_status()` — —
- `encoder_debug()` — —
- `encoder_activate()` — —
- `encoder_deactivate()` — —
- `encoder_scroll()` — —
- `encoder_click()` — —
- `encoder_cancel()` — —
- `encoder_focus()` — —
- `encoder_page_next()` — —
- `encoder_page_prev()` — —
- `dashboard_page()` — —
- `dashboard_action_page()` — —
- `files_page()` — —
- `files_with_path_page()` — —
- `files_with_table_page()` — —
- `files_with_action_page()` — —
- `docs_page()` — —
- `client_js()` — —
- `c2004_test_page()` — —
- `anyio_backend()` — —
- `test_index_page()` — —
- `test_dashboard_page()` — —
- `test_encoder_status_no_browser()` — —
- `test_oql_files_endpoint()` — —
- `test_hub_is_initialized()` — —
- `cprint()` — —
- `api_call()` — —
- `get()` — —
- `post()` — —
- `cmd()` — —
- `cmd_status()` — —
- `cmd_activate()` — —
- `cmd_deactivate()` — —
- `cmd_scroll()` — —
- `cmd_click()` — —
- `cmd_cancel()` — —
- `cmd_focus()` — —
- `cmd_page_next()` — —
- `cmd_page_prev()` — —
- `cmd_run()` — —
- `cmd_help()` — —
- `print_status()` — —
- `run_oql_file()` — —
- `shell()` — —
- `api_get()` — —
- `api_post()` — —
- `check_browser_connected()` — —
- `test_files_page()` — —
- `test_docs_page()` — —
- `test_api_docs_page()` — —
- `test_client_js()` — —
- `test_encoder_activate_no_browser()` — —
- `test_encoder_scroll_no_browser()` — —
- `test_encoder_click_no_browser()` — —
- `test_encoder_cancel_no_browser()` — —
- `test_encoder_focus_no_browser()` — —
- `test_encoder_page_next_no_browser()` — —
- `test_encoder_page_prev_no_browser()` — —
- `test_oql_list_files()` — —
- `test_oql_read_file()` — —
- `test_oql_read_file_legacy_c2004_path()` — —
- `test_oql_read_file_not_found()` — —
- `test_oql_read_file_path_traversal()` — —
- `test_oql_run_line_comment()` — —
- `test_oql_run_line_wait()` — —
- `test_oql_run_line_set()` — —
- `test_oql_run_line_unknown()` — —
- `test_oql_run_line_encoder_scroll()` — —
- `test_oql_run_line_encoder_focus()` — —
- `test_hub_no_clients()` — —
- `test_hub_disconnect_nonexistent()` — —
- `history()` — —
- `id_health()` — —
- `test_module_health_endpoint_reports_connect_id_module()` — —
- `repo()` — —
- `handler()` — —
- `test_identify_user_rfid()` — —
- `test_identify_invalid_rfid()` — —
- `test_identify_qr()` — —
- `test_clear_history()` — —
- `test_identification_is_relayed_to_subscriber_inbox()` — —
- `create_schedule()` — —
- `list_schedules()` — —
- `manager_health()` — —
- `test_module_health_endpoint_reports_connect_manager_module()` — —
- `test_capabilities_lists_expected_endpoints()` — —
- `create_handler()` — —
- `update_handler()` — —
- `schedule_handler()` — —
- `delete_handler()` — —
- `list_handler()` — —
- `get_handler()` — —
- `test_create_assignment()` — —
- `test_update_assignment_status()` — —
- `test_update_assignment_status_not_found()` — —
- `test_schedule_periodic_test()` — —
- `test_delete_assignment()` — —
- `test_delete_assignment_not_found()` — —
- `test_list_assignments_query()` — —
- `test_list_assignments_filtered_by_status()` — —
- `test_get_assignment_query()` — —
- `test_get_assignment_query_not_found()` — —
- `test_inbox_event_triggers_identify_to_assignment_saga()` — —
- `saga_setup()` — —
- `test_saga_creates_assignment_for_user_with_hint()` — —
- `test_saga_skips_event_without_hint()` — —
- `test_saga_ignores_non_user_identifications()` — —
- `test_saga_is_idempotent_on_correlation_id()` — —
- `test_identify_to_assignment_saga_creates_assignment_for_user()` — —
- `test_identify_to_assignment_saga_ignores_non_user()` — —
- `test_identify_to_assignment_saga_is_idempotent_by_correlation_id()` — —
- `protocol_health()` — —
- `test_protocol_lifecycle_through_api()` — —
- `test_list_protocols_filters_by_status()` — —
- `publish_scenario()` — —
- `add_activity()` — —
- `hardware_health_proxy()` — —
- `hardware_identify_proxy()` — —
- `hardware_proxy_info()` — —
- `hardware_diagnostic_command()` — —
- `scenario_health()` — —
- `test_module_health_endpoint_reports_connect_scenario_module()` — —
- `test_scenario_survives_new_handler()` — —
- `test_publish_and_list_scenarios()` — —
- `test_activities_persisted()` — —
- `publish_handler()` — —
- `add_activity_handler()` — —
- `test_create_scenario()` — —
- `test_update_scenario()` — —
- `test_publish_and_add_activity()` — —
- `test_update_scenario_not_found()` — —
- `health_full()` — —
- `bridge_scenario()` — —
- `parse_endpoint()` — —
- `serialize_endpoint()` — —
- `validate_endpoint()` — —
- `exec_endpoint()` — —
- `scenario_build_endpoint()` — —
- `xsd_endpoint()` — —
- `json_schema_endpoint()` — —
- `validate_ast_endpoint()` — —
- `dsl_to_xml_endpoint()` — —
- `ast_to_xml_endpoint()` — —
- `xml_to_ast_endpoint()` — —
- `validate_dsl_text_endpoint()` — —
- `xml_migrate_endpoint()` — —
- `xml_split_endpoint()` — —
- `list_scenario_files()` — —
- `get_scenario_file()` — —
- `save_scenario_file()` — —
- `configure_maskservice_client()` — —
- `close_maskservice_client()` — —
- `get_maskservice_base_url()` — —
- `fetch_scenario()` — —
- `normalize_dsl()` — —
- `normalize_quoted_dsl_line()` — —
- `render_legacy_task_as_dsl_lines()` — —
- `dsl_from_scenario_content()` — —
- `goals_from_content()` — —
- `execute_ast()` — —
- `execute_dsl()` — —
- `highlight_line()` — —
- `highlight_dsl()` — —
- `migrate_legacy_xml_to_dsl()` — —
- `split_legacy_xml_to_scenarios()` — —
- `parse_dsl()` — —
- `normalize_dsl_line_quotes()` — —
- `normalize_dsl_text_quotes()` — —
- `format_dsl_literal()` — —
- `quote_dsl_value()` — —
- `canonicalize_dsl_line_quotes()` — —
- `canonicalize_dsl_quotes()` — —
- `read_quoted_token()` — —
- `get_json_schema()` — —
- `validate_ast()` — —
- `ast_to_dsl_text()` — —
- `normalize_dsl_text()` — —
- `validate_dsl_text()` — —
- `validate_dsl_format()` — —
- `ast_to_xml()` — —
- `dsl_to_xml()` — —
- `xml_to_ast()` — —
- `test_health()` — —
- `test_capabilities_lists_implemented_and_no_stubs()` — —
- `test_quote()` — —
- `test_quote_apostrophe_uses_double()` — —
- `test_unquote()` — —
- `test_canonicalize()` — —
- `test_highlight_basic_keywords_are_wrapped()` — —
- `test_parse_returns_ast()` — —
- `test_validate_returns_result()` — —
- `test_exec_returns_plan()` — —
- `test_serialize_returns_text()` — —
- `test_scenario_build_returns_dsl()` — —
- `test_xsd_endpoint_returns_static_definition()` — —
- `test_json_schema_endpoint_matches_root_shape()` — —
- `test_validate_ast_accepts_minimal_valid_ast()` — —
- `test_validate_ast_rejects_missing_scenario()` — —
- `test_validate_ast_rejects_empty_goals_and_funcs()` — —
- `test_ast_to_xml_returns_indented_xml()` — —
- `test_dsl_to_xml_round_trips_via_xml_to_ast()` — —
- `test_xml_to_ast_rejects_missing_root()` — —
- `test_xml_to_ast_rejects_invalid_xml()` — —
- `test_dsl_to_xml_returns_errors_on_bad_input()` — —
- `test_validate_dsl_text_succeeds_for_valid_dsl()` — —
- `test_validate_dsl_text_collects_parse_errors()` — —
- `test_xml_migrate_handles_native_xml()` — —
- `test_xml_migrate_handles_legacy_report()` — —
- `test_xml_migrate_returns_errors_on_unrelated_xml()` — —
- `test_xml_split_returns_one_scenario_per_transaction()` — —
- `test_xml_split_returns_empty_on_native_xml()` — —
- `test_health_full_reports_unreachable_when_maskservice_down()` — —
- `test_health_full_reports_ok_when_maskservice_returns_200()` — —
- `test_bridge_scenario_returns_upstream_record()` — —
- `test_bridge_scenario_returns_404_when_upstream_404()` — —
- `test_bridge_scenario_returns_503_when_upstream_unreachable()` — —
- `test_exec_simple_dsl()` — —
- `test_exec_with_condition()` — —
- `test_exec_with_log()` — —
- `test_exec_returns_ast()` — —
- `test_parse_simple_scenario()` — —
- `test_parse_with_condition()` — —
- `test_parse_with_task()` — —
- `test_parse_empty_returns_error()` — —
- `test_parse_with_func()` — —
- `test_parse_with_wait()` — —
- `test_format_dsl_literal_defaults_to_single_quotes()` — —
- `test_format_dsl_literal_falls_back_to_double_quotes_with_apostrophe()` — —
- `test_quote_dsl_value_handles_none()` — —
- `test_normalize_line_single_to_double()` — —
- `test_normalize_text_multiline()` — —
- `test_canonicalize_line_double_to_single()` — —
- `test_canonicalize_preserves_double_quotes_for_values_with_apostrophes()` — —
- `test_canonicalize_text_multiline()` — —
- `test_read_quoted_token_single()` — —
- `test_read_quoted_token_double()` — —
- `test_read_quoted_token_bare()` — —
- `test_build_dsl_from_test_scenario()` — —
- `test_build_goals_from_test_scenario()` — —
- `test_build_dsl_from_generic_scenario()` — —
- `test_build_dsl_with_duration()` — —
- `test_serialize_simple_ast()` — —
- `test_serialize_with_set_step()` — —
- `test_serialize_with_if_condition()` — —
- `test_validate_valid_dsl()` — —
- `test_validate_detects_missing_scenario()` — —
- `test_validate_detects_placeholder_star()` — —
- `test_validate_returns_fixed_text()` — —
- `test_validate_detects_missing_get()` — —
- `create_template()` — —
- `update_template()` — —
- `create_draft()` — —
- `get_template()` — —
- `list_templates()` — —
- `get_service()` — —
- `delete_template()` — —
- `list_json_data()` — —
- `create_json_data()` — —
- `update_json_data()` — —
- `delete_json_data()` — —
- `preview_template()` — —
- `test_preview_endpoint_uses_service_override()` — —
- `test_template_survives_new_handler()` — —
- `test_publish_and_filter_templates()` — —
- `test_create_template()` — —
- `test_update_template()` — —
- `test_publish_template()` — —
- `test_update_nonexistent()` — —
- `test_save_draft_creates_new_record_without_stable_lookup_keys()` — —
- `test_save_draft_updates_existing_record_for_matching_preset()` — —
- `test_publish_template_increments_version_and_marks_published()` — —
- `test_render_preview_resolves_svg_paths_and_clears_unknown_placeholders()` — —
- `start_session()` — —
- `complete_step()` — —
- `finalize_session()` — —
- `list_sessions()` — —
- `get_session_history()` — —
- `pause_session()` — —
- `resume_session()` — —
- `cancel_session()` — —
- `test_module_health_endpoint_reports_connect_test_module()` — —
- `start_handler()` — —
- `step_handler()` — —
- `finalize_handler()` — —
- `test_start_session()` — —
- `test_complete_step()` — —
- `test_finalize_session()` — —
- `test_step_without_session()` — —
- `generate_individual()` — —
- `generate_sprite()` — —
- `extract_symbols()` — —
- `count_symbols()` — —
- `add_MenuCommandServiceServicer_to_server()` — —
- `add_MenuQueryServiceServicer_to_server()` — —
- `deploy()` — —
- `get_manifest()` — —
- `list_pages()` — —
- `serve_ui()` — —
- `serve_model()` — —
- `list_models()` — —
- `index()` — —
- `module_index()` — —
- `curl_json()` — —
- `curl_status()` — —
- `curl_text()` — —
- `ssh_cmd()` — —
- `check_reachable()` — —
- `check_backend_health()` — —
- `check_firmware_health()` — —
- `check_frontend_html()` — —
- `check_config_js()` — —
- `check_api_menu()` — —
- `check_api_config_sections()` — —
- `check_api_auth_session()` — —
- `check_api_state()` — —
- `check_db_counts()` — —
- `check_proxy_passthrough()` — —
- `check_proxy_firmware()` — —
- `check_js_bundles()` — —
- `check_kiosk_browser()` — —
- `check_screen_resolution()` — —
- `check_services_running()` — —
- `check_data_sync()` — —
- `run_checks()` — —
- `compare_targets()` — —
- `create_health_router()` — —
- `build_version_payload()` — —
- `create_version_router()` — —
- `verify_password()` — —
- `create_command_router()` — —
- `create_query_router()` — —
- `result_to_response()` — —
- `create_inbox_router()` — —
- `setup_cqrs()` — —
- `reset_cqrs_setup()` — —
- `build_alembic_env_main()` — —
- `query_events()` — —
- `append_events_to_stream()` — —
- `list_event_streams()` — —
- `clear_event_stream()` — —
- `create_logs_router()` — —
- `resolve_logs_db_path()` — —
- `build_logs_query_service()` — —
- `item_to_command()` — —
- `column_to_command()` — —
- `columns_to_commands()` — —
- `route_mappings_to_commands()` — —
- `delete_menu_item_from_db()` — —
- `list_menu_modules()` — —
- `broadcast_menu_update()` — —
- `define_menu_models()` — —
- `create_nfo_setup()` — —
- `clean_version()` — —
- `resolve_release_version()` — —
- `get_pressure_service()` — —
- `create_sensors_router()` — —
- `parse_args()` — —
- `is_ignored()` — —
- `iter_python_files()` — —
- `is_within()` — —
- `module_parts_for()` — —
- `build_workspace_index()` — —
- `pick_best()` — —
- `resolve_absolute()` — —
- `resolve_relative()` — —
- `classify_external()` — —
- `edge_bucket()` — —
- `build_yaml()` — —
- `yaml_scalar()` — —
- `to_module_label()` — —
- `normalize_edges()` — —
- `format_top_list()` — —
- `render_toon()` — —
- `write_payloads()` — —
- `analyze()` — —
- `log_error()` — —
- `log_success()` — —
- `log_warning()` — —
- `log_info()` — —
- `log_header()` — —
- `build_import_graph()` — —
- `find_circular_imports()` — —
- `analyze_module_coupling()` — —
- `calculate_import_depth()` — —
- `generate_dot_graph()` — —
- `find_all_ts_files()` — —
- `get_modules()` — —
- `get_pages()` — —
- `count_references()` — —
- `find_external_apis()` — —
- `find_large_files()` — —
- `analyze_module_usage()` — —
- `analyze_page_usage()` — —
- `find_similar_files()` — —
- `find_refactor_candidates()` — —
- `backup_database()` — —
- `get_tables()` — —
- `drop_table()` — —
- `consolidate_scenario_db()` — —
- `consolidate_root_identification_db()` — —
- `consolidate_app_db()` — —
- `update_backend_db_config()` — —
- `convert_dsl_syntax()` — —
- `find_duplicate_code_blocks()` — —
- `find_unused_imports_ts()` — —
- `find_unused_imports_py()` — —
- `find_todos()` — —
- `find_console_logs()` — —
- `find_all_databases()` — —
- `get_table_info()` — —
- `compare_schemas()` — —
- `find_duplicates()` — —
- `analyze_duplicates()` — —
- `generate_migration_sql()` — —
- `find_all_exports_ts()` — —
- `find_all_imports_ts()` — —
- `find_unused_files()` — —
- `find_empty_functions()` — —
- `should_skip()` — —
- `find_python_models()` — —
- `find_typescript_models()` — —
- `scan_directory()` — —
- `print_report()` — —
- `should_skip_name()` — —
- `is_component_specific_name()` — —
- `is_component_specific_path()` — —
- `classify_type()` — —
- `should_skip_path()` — —
- `find_enums()` — —
- `find_dataclasses()` — —
- `find_interfaces()` — —
- `scan_project()` — —
- `find_shared_interfaces()` — —
- `print_priority_report()` — —
- `generate_revision_id()` — —
- `generate_migration_file()` — —
- `cmd_add_column()` — —
- `cmd_drop_column()` — —
- `cmd_rename_column()` — —
- `cmd_add_table()` — —
- `cmd_drop_table()` — —
- `cmd_add_index()` — —
- `cmd_add_fk()` — —
- `save_migration()` — —
- `get_file_category()` — —
- `is_test_file()` — —
- `is_cqrs_file()` — —
- `count_lines()` — —
- `detect_layer()` — —
- `infer_source_from_test()` — —
- `extract_imports()` — —
- `resolve_import_path()` — —
- `build_dependency_graph()` — —
- `find_cycles()` — —
- `calculate_coupling_metrics()` — —
- `detect_module_boundaries()` — —
- `extract_module_name()` — —
- `analyze_typescript_deps()` — —
- `find_singleton_files()` — —
- `find_global_instances()` — —
- `find_shared_state()` — —
- `analyze_singleton_dependencies()` — —
- `calculate_singleton_metrics()` — —
- `generate_di_recommendations()` — —
- `read_file_safe()` — —
- `extract_module_from_path()` — —
- `extract_exports()` — —
- `extract_singleton_deps()` — —
- `is_at_module_level()` — —
- `analyze_singletons()` — —
- `find_cqrs_directories()` — —
- `analyze_cqrs_completeness()` — —
- `find_modules_without_cqrs()` — —
- `extract_event_handlers()` — —
- `analyze_handler_coverage()` — —
- `find_all_events()` — —
- `analyze_cqrs_consistency()` — —
- `calculate_consistency_score()` — —
- `generate_cqrs_recommendations()` — —
- `extract_parent_module()` — —
- `group_by_module()` — —
- `analyze_cqrs()` — —
- `analyze_syntactic_debt()` — —
- `analyze_structural_debt()` — —
- `analyze_semantic_debt()` — —
- `analyze_architectural_debt()` — —
- `decompose_file()` — —
- `decompose_project()` — —
- `load_json_safe()` — —
- `generate_executive_summary()` — —
- `generate_task_list()` — —
- `generate_llm_prompts()` — —
- `generate_verification_checklist()` — —
- `generate_final_report()` — —
- `validate_dsl()` — —
- `parse_summary()` — —
- `generate_html_report()` — —
- `generate_markdown_report()` — —
- `extract_backend_endpoints()` — —
- `extract_frontend_api_calls()` — —
- `normalize_path()` — —
- `find_deprecated_patterns()` — —
- `validate_endpoints()` — —
- `extract_tables_from_models()` — —
- `extract_tables_from_api()` — —
- `get_db_tables()` — —
- `generate_create_table_sql()` — —
- `validate_schema()` — —
- `parse_sqlalchemy_models()` — —
- `get_db_schema()` — —
- `normalize_type()` — —
- `compare_types()` — —
- `validate_models()` — —
- `extract_typescript_interfaces()` — —
- `parse_ts_fields()` — —
- `extract_python_models()` — —
- `parse_python_fields()` — —
- `normalize_type_name()` — —
- `find_matching_types()` — —
- `validate_types()` — —
- `strip_nfo_fallback_blocks()` — —
- `parse_auto_log_modules()` — —
- `get_module_name_for_file()` — —
- `check_nfo_in_requirements()` — —
- `check_setup_nfo_in_main()` — —
- `audit_file()` — —
- `collect_py_files()` — —
- `audit_service()` — —
- `c()` — —
- `print_separator()` — —
- `print_header()` — —
- `print_service_report()` — —
- `print_summary()` — —
- `output_json()` — —
- `bootstrap()` — —
- `find_emoji_in_line()` — —
- `scan_app_files()` — —
- `is_emoji()` — —
- `find_emoji_in_file()` — —
- `scan_frontend_directory()` — —
- `parse_line()` — —
- `parse_script()` — —
- `find_svg_tokens()` — —
- `build_output()` — —
- `profile_get_section()` — —
- `profile_health()` — —
- `profile_auth_session()` — —
- `profile_auth_demo_users()` — —
- `profile_menu_configurations()` — —
- `profile_menu_sidebar()` — —
- `profile_menu_modules()` — —
- `profile_connect_menu_api()` — —
- `profile_config_sections()` — —
- `profile_config_system()` — —
- `profile_data_tables()` — —
- `profile_scenarios()` — —
- `profile_reports()` — —
- `profile_template_json()` — —
- `profile_devtools_scripts()` — —
- `profile_version()` — —
- `profile_diagnostics()` — —
- `profile_workshops()` — —
- `profile_test_sessions()` — —
- `profile_assignments()` — —
- `profile_activities()` — —
- `profile_startup_sequence()` — —
- `profile_navigation_sequence()` — —
- `profile_concurrent_startup()` — —
- `migrate_table()` — —
- `migrate()` — —
- `normalize_icon_token()` — —
- `collect_changes()` — —
- `apply_changes()` — —
- `apply_changes_limited()` — —
- `patch_history_patterns()` — —
- `patch_url_fallback()` — —
- `patch_apply_step()` — —
- `compute_scores()` — —
- `format_report()` — —
- `perf_recommendations()` — —
- `code_quality_recommendations()` — —
- `run_backend_tests()` — —
- `run_api_tests()` — —
- `run_frontend_tests()` — —
- `run_connect_data_tests()` — —
- `run_performance_profile()` — —
- `run_code_quality()` — —
- `run_command()` — —
- `refactor_file()` — —
- `remove_inline_styles()` — —
- `process_file()` — —
- `log()` — —
- `load_config()` — —
- `save_config()` — —
- `get_hourly_activity()` — —
- `find_optimal_restart_hour()` — —
- `compute_optimal_restart_time()` — —
- `load_schedule()` — —
- `save_schedule()` — —
- `compute_next_restart()` — —
- `schedule_restart()` — —
- `send_notification_to_frontend()` — —
- `notify_restart()` — —
- `is_test_in_progress()` — —
- `was_cancel_requested()` — —
- `cancel_restart()` — —
- `execute_restart()` — —
- `check_startup_restart()` — —
- `daemon_loop()` — —
- `show_status()` — —
- `write_if_missing()` — —
- `package_json()` — —
- `tsconfig_json()` — —
- `push_to_api()` — —
- `generate_dsl()` — —
- `generate_cql()` — —
- `generate_goals_json()` — —
- `parse_xml()` — —
- `is_pump_output()` — —
- `is_compressor_output()` — —
- `normalize_output_name()` — —
- `normalize_flow_value()` — —
- `normalize_set_value()` — —
- `pbkdf2_hash()` — —
- `now_iso()` — —
- `seed_users()` — —
- `get_db_url()` — —
- `run_db_seeds()` — —
- `run_alembic_upgrade()` — —
- `list_all_seeds()` — —
- `get_admin_role_id()` — —
- `get_all_routes()` — —
- `set_admin_full_access()` — —
- `verify_admin_access()` — —
- `get_system_info()` — —
- `get_test_stats()` — —
- `get_restart_stats()` — —
- `record_restart()` — —
- `collect_daily_stats()` — —
- `upload_stats()` — —
- `upload_pending_stats()` — —
- `copy_to_pendrive()` — —
- `get_identification_service()` — —
- `patch_row()` — —
- `get_table_columns()` — —
- `startup_event()` — —
- `shutdown_event()` — —
- `ensure_demo_schema()` — —
- `list_tables()` — —
- `list_columns()` — —
- `get_rows()` — —
- `create_protocol()` — —
- `get_protocols()` — —
- `update_protocol()` — —
- `save_protocol()` — —
- `delete_protocol()` — —
- `should_include_page()` — —
- `extract_page_info()` — —
- `generate_encoder_test()` — —
- `generate_api_test()` — —
- `parse_makefile()` — —
- `parse_taskfile()` — —
- `parse_docker_compose()` — —
- `parse_buf_yaml()` — —
- `generate_testql_scenario()` — —
- `parse_router_prefix()` — —
- `parse_endpoints()` — —
- `collect_all_endpoints()` — —
- `collect_covered_endpoints()` — —
- `current_iteration()` — —
- `assign_wave()` — —
- `generate_scenario()` — —
- `extract_pages()` — —
- `ensure_dir()` — —
- `is_internal()` — —
- `crawl_routes()` — —
- `test_dashboard_url_navigation()` — —
- `test_files_url_navigation()` — —
- `test_url_structure()` — —
- `test_url_navigation()` — —
- `run_scenario()` — —
- `list_peripherals()` — —
- `test_iframe_pilot_toggle_smoke_off_on_and_child_shell_only()` — —
- `test_iframe_pilot_toggle_preserves_authenticated_session()` — —
- `test_service_id_pilot_guardrails_health_and_rollback_toggle()` — —
- `test_service_id_pilot_routing_toggle_smoke()` — —
- `test_service_id_session_continuity_and_cors_across_toggle()` — —
- `test_service_manager_pilot_routing_toggle_smoke()` — —
- `test_checked_in_menu_proto_artifacts_are_importable()` — —
- `test_menu_proto_contract_compiles()` — —
- `test_generated_menu_proto_modules_are_importable()` — —
- `test_normalize_icon_token_handles_svg_emoji_html_and_names()` — —
- `test_collect_changes_builds_filters_and_returns_only_changed_rows()` — —
- `test_collect_changes_supports_limit_and_offset()` — —
- `test_apply_changes_runs_update_per_row()` — —
- `test_apply_changes_limited_caps_updates()` — —
- `test_apply_changes_limited_zero_means_no_updates()` — —
- `page_file()` — —
- `test_resolve_protogate_python_prefers_explicit_path()` — —
- `test_resolve_protogate_python_uses_env_candidate()` — —
- `test_resolve_protogate_python_rejects_invalid_explicit()` — —
- `test_resolve_protogate_python_skips_non_executable_env_candidate()` — —
- `test_main_builds_expected_discovery_command()` — —
- `test_main_returns_error_when_explicit_python_invalid()` — —
- `test_parse_args_uses_canonical_default_output_dir()` — —
- `load_docs()` — —
- `ref_name()` — —
- `py_type()` — —
- `maybe_optional()` — —
- `with_default()` — —
- `render_anyof_validator()` — —
- `render_class()` — —
- `render_doc()` — —
- `render_module()` — —
- `render_init()` — —
- `ts_lit()` — —
- `ref_schema_name()` — —
- `zod_expr()` — —
- `render_defs()` — —
- `render_validators()` — —
- `validate_schema_file()` — —
- `collect_schema_files()` — —
- `add_ConnectDataCommandServiceServicer_to_server()` — —
- `add_ConnectDataQueryServiceServicer_to_server()` — —
- `add_ConnectIdCommandServiceServicer_to_server()` — —
- `add_ConnectIdQueryServiceServicer_to_server()` — —
- `add_ConnectScenarioCommandServiceServicer_to_server()` — —
- `add_ConnectScenarioQueryServiceServicer_to_server()` — —
- `add_ConnectTestCommandServiceServicer_to_server()` — —
- `add_ConnectTestQueryServiceServicer_to_server()` — —
- `add_ConnectTestProtocolCommandServiceServicer_to_server()` — —
- `add_ConnectTestProtocolQueryServiceServicer_to_server()` — —
- `register_logs_handlers()` — —
- `read_env_lines()` — —
- `parse_env()` — —
- `render_html_page()` — —
- `ui_root()` — —
- `save_env()` — —
- `init()` — —
- `walk()` — —
- `restore()` — —
- `report()` — —
- `dashboard()` — —
- `accelerator()` — —
- `serve()` — —
- `tui()` — —
- `version()` — —
- `duplicates()` — —
- `vector_build()` — —
- `vector_query()` — —
- `multi_repo()` — —
- `services()` — —
- `truth()` — —
- `plan()` — —
- `pr()` — —
- `execute()` — —
- `get_cc_for_day()` — —
- `generate_dashboard()` — —
- `launch_tui()` — —
- `test_run_aborts_when_baseline_snapshot_creation_fails()` — —
- `test_run_day_fast_stops_when_db_restore_fails()` — —
- `test_run_day_fast_stops_when_app_health_fails_after_db_restore()` — —
- `test_run_day_fast_uses_full_health_gate_after_db_restore()` — —
- `test_prewarm_worktrees_calls_get_or_create_for_every_sha()` — —
- `test_prewarm_worktrees_continues_after_individual_failure()` — —
- `test_needs_db_restore_returns_true_when_migration_file_changed()` — —
- `test_needs_db_restore_returns_false_when_no_db_files_changed()` — —
- `test_needs_db_restore_returns_true_when_no_previous_commit()` — —
- `test_needs_db_restore_returns_true_when_diff_fails()` — —
- `test_run_day_fast_skips_db_restore_when_no_db_files_changed()` — —
- `test_needs_rescan_returns_true_when_no_cache()` — —
- `test_needs_rescan_returns_true_when_route_file_changed()` — —
- `test_needs_rescan_returns_false_when_only_non_route_files_changed()` — —
- `test_needs_rescan_returns_true_when_diff_fails()` — —
- `test_run_day_fast_reuses_cached_endpoints_when_no_route_changes()` — —
- `test_diff_names_cached_calls_git_only_once_for_same_pair()` — —
- `test_diff_names_cached_returns_lowercased_paths()` — —
- `test_switch_commit_syncs_code_when_live_bind_swap_is_disabled()` — —
- `test_switch_commit_sets_current_sha_before_triggering_reload()` — —
- `test_prepare_runtime_creates_runtime_compose_and_active_link()` — —
- `test_accelerated_compose_up_uses_runtime_compose_override()` — —
- `test_switch_commit_uses_short_health_gate_even_with_long_config_timeout()` — —
- `test_switch_commit_skips_short_health_probe_when_recent_success_is_cached()` — —
- `test_switch_commit_rechecks_health_when_cache_is_stale()` — —
- `test_get_container_name_reuses_cached_container_lookup()` — —
- `test_get_container_name_caches_compose_ps_fallback_result()` — —
- `test_trigger_reload_caches_signal_strategy_after_first_success()` — —
- `test_sync_code_incremental_when_diff_is_small()` — —
- `test_sync_code_falls_back_to_full_copy_when_diff_exceeds_threshold()` — —
- `test_trigger_reload_caches_exec_strategy_after_signal_failure()` — —
- `test_start_dry_run_skips_deploy()` — —
- `test_start_none_method_returns_true()` — —
- `test_stop_dry_run_skips()` — —
- `test_stop_none_method_skips()` — —
- `test_start_none_returns_true()` — —
- `test_compose_file_finds_yml()` — —
- `test_compose_file_finds_yaml()` — —
- `test_compose_file_explicit()` — —
- `test_days_with_commits_parses_log()` — —
- `test_days_with_commits_filters_by_date_range()` — —
- `test_days_with_commits_empty_on_error()` — —
- `test_checkout_calls_git()` — —
- `test_restore_head_calls_git()` — —
- `test_restore_head_with_sha()` — —
- `test_clone_for_walk_creates_clone()` — —
- `test_execute_delegates_to_days_with_commits()` — —
- `test_load_history_empty_dir()` — —
- `test_load_history_no_results_json()` — —
- `test_load_history_single_day()` — —
- `test_load_history_multiple_days_sorted()` — —
- `test_load_history_health_pct()` — —
- `test_load_history_skips_invalid_dir_name()` — —
- `test_load_history_with_commit()` — —
- `test_load_history_status_timeout()` — —
- `test_load_history_testql_passed()` — —
- `test_endpoint_url()` — —
- `test_endpoint_url_strips_trailing_slash()` — —
- `test_endpoint_slug()` — —
- `test_day_result_health_pct_empty()` — —
- `test_day_result_health_pct()` — —
- `test_walk_config_defaults()` — —
- `test_run_returns_empty_when_no_commits()` — —
- `test_run_day_dry_run_skips_checkout()` — —
- `test_run_day_returns_day_result()` — —
- `test_run_day_deploy_failure_skips_scan()` — —
- `test_run_day_stop_always_called()` — —
- `test_run_processes_all_days()` — —
- `test_save_day_creates_results_file()` — —
- `test_save_day_creates_yaml_and_toon()` — —
- `test_save_day_includes_commit_in_json()` — —
- `test_save_day_no_output_dir_skips()` — —
- `test_save_day_health_in_json()` — —
- `test_save_html_creates_report_file()` — —
- `test_save_html_contains_health_pct()` — —
- `test_save_timeline_index_creates_index()` — —
- `test_save_timeline_index_multiple_days_sorted()` — —
- `test_find_last_working_day_found()` — —
- `test_find_last_working_day_picks_most_recent()` — —
- `test_find_last_working_day_none_when_always_fail()` — —
- `test_find_last_working_day_missing_dir()` — —
- `test_find_last_working_day_ignores_other_endpoints()` — —
- `test_find_last_working_day_skips_invalid_dirs()` — —
- `test_execute_returns_date()` — —
- `test_parse_openapi_returns_endpoints()` — —
- `test_parse_openapi_ignores_unknown_methods()` — —
- `test_parse_openapi_empty_paths()` — —
- `test_scan_via_compose_labels_finds_traefik_prefix()` — —
- `test_scan_via_compose_labels_no_compose_file()` — —
- `test_scan_via_compose_labels_dict_labels()` — —
- `test_ports_to_endpoints()` — —
- `test_ports_to_endpoints_no_services()` — —
- `test_execute_falls_back_to_health()` — —
- `test_execute_deta_takes_priority()` — —
- `test_execute_deduplicates_openapi_vs_deta()` — —
- `test_test_service_ok()` — —
- `test_test_service_timeout()` — —
- `test_test_service_set_day_dir()` — —
- `print()` — —
- `send_mvp_command()` — —
- `print()` — —
- `sendMVPCommand()` — —
- `health()` — —
- `get_manifest()` — —
- `list_pages()` — —
- `serve_ui(filename)` — —
- `serve_model(filename)` — —
- `list_models()` — —
- `index()` — —
- `module_index()` — —
- `health()` — —
- `get_manifest()` — —
- `list_pages()` — —
- `serve_ui(filename)` — —
- `serve_model(filename)` — —
- `list_models()` — —
- `index()` — —
- `module_index()` — —
- `health()` — —
- `get_manifest()` — —
- `list_pages()` — —
- `serve_ui(filename)` — —
- `serve_model(filename)` — —
- `list_models()` — —
- `index()` — —
- `module_index()` — —
- `health()` — —
- `get_manifest()` — —
- `list_pages()` — —
- `serve_ui(filename)` — —
- `serve_model(filename)` — —
- `list_models()` — —
- `index()` — —
- `module_index()` — —
- `marked()` — —
- `manifest()` — —
- `sidebar()` — —
- `content()` — —
- `getRoute()` — —
- `hash()` — —
- `navigate()` — —
- `handleRoute()` — —
- `route()` — —
- `docPath()` — —
- `updateActiveNav()` — —
- `page()` — —
- `renderHome()` — —
- `renderDownloads()` — —
- `renderArchitecture()` — —
- `renderServices()` — —
- `renderServicesGrid()` — —
- `grid()` — —
- `isActive()` — —
- `coreClass()` — —
- `checkServiceHealth()` — —
- `start()` — —
- `controller()` — —
- `timeout()` — —
- `resp()` — —
- `time()` — —
- `runHealthCheck()` — —
- `summary()` — —
- `timeEl()` — —
- `healthyEl()` — —
- `unhealthyEl()` — —
- `table()` — —
- `allServices()` — —
- `health()` — —
- `dot()` — —
- `healthy()` — —
- `unhealthy()` — —
- `degraded()` — —
- `statusClass()` — —
- `statusText()` — —
- `loadDockerStatus()` — —
- `container()` — —
- `renderDocs()` — —
- `res()` — —
- `md()` — —
- `html()` — —
- `renderSidebar()` — —
- `esc()` — —
- `d()` — —
- `all()` — —
- `generate_readme()` — —
- `marked()` — —
- `manifest()` — —
- `sidebar()` — —
- `content()` — —
- `getRoute()` — —
- `hash()` — —
- `navigate()` — —
- `handleRoute()` — —
- `route()` — —
- `docPath()` — —
- `updateActiveNav()` — —
- `page()` — —
- `renderHome()` — —
- `renderDownloads()` — —
- `renderArchitecture()` — —
- `renderServices()` — —
- `renderServicesGrid()` — —
- `grid()` — —
- `isActive()` — —
- `coreClass()` — —
- `checkServiceHealth()` — —
- `start()` — —
- `controller()` — —
- `timeout()` — —
- `resp()` — —
- `time()` — —
- `runHealthCheck()` — —
- `summary()` — —
- `timeEl()` — —
- `healthyEl()` — —
- `unhealthyEl()` — —
- `table()` — —
- `allServices()` — —
- `health()` — —
- `dot()` — —
- `healthy()` — —
- `unhealthy()` — —
- `degraded()` — —
- `statusClass()` — —
- `statusText()` — —
- `loadDockerStatus()` — —
- `container()` — —
- `renderDocs()` — —
- `res()` — —
- `md()` — —
- `html()` — —
- `renderSidebar()` — —
- `esc()` — —
- `d()` — —
- `get_manifest()` — —
- `list_pages()` — —
- `serve_ui()` — —
- `serve_model()` — —
- `list_models()` — —
- `index()` — —
- `module_index()` — —
- `all()` — —
- `run_migration()` — —
- `main()` — —
- `set_default()` — —
- `add_to_api_main()` — —
- `add_to_backend_main()` — —
- `create_performance_demo()` — —
- `create_requirements()` — —
- `slow_function()` — —
- `async_slow_function()` — —
- `simulate_api_calls()` — —
- `simulate_queries()` — —
- `create_api_timing_middleware()` — —
- `setup_performance_monitoring()` — —
- `create_db_profiling_engine()` — —
- `profile_function()` — —
- `profile_async_function()` — —
- `get_database_url()` — —
- `run_migrations_offline()` — —
- `run_migrations_online()` — —
- `upgrade()` — —
- `downgrade()` — —
- `get_source_db_path()` — —
- `include_object()` — —
- `ensure_device()` — —
- `ensure_customer()` — —
- `ensure_workshop()` — —
- `seed_template_variables()` — —
- `parse_date()` — —
- `parse_xml_protocol()` — —
- `seed()` — —
- `build_scenario()` — —
- `gen_scenario_id()` — —
- `upsert_scenario()` — —
- `seed_scenarios_list()` — —
- `run_seeds()` — —
- `list_seeds()` — —
- `mem_engine()` — —
- `generate_js_definition()` — —
- `safe_drop_table()` — —
- `noop_downgrade()` — —
- `safe_drop_column()` — —
- `table_exists()` — —
- `has_column()` — —
- `ensure_table()` — —
- `run_safely()` — —
- `resolve_dv_xml_path()` — —
- `read_vars_from_xml()` — —
- `insert_if_missing()` — —
- `safe_query()` — —
- `safe_query_scalar()` — —
- `load_legacy_table()` — —
- `create_table_if_missing()` — —
- `normalize_string()` — —
- `early_return_if_table_missing()` — —
- `get_db()` — —
- `get_config_db()` — —
- `get_menu_db()` — —
- `get_command_bus()` — —
- `get_query_bus()` — —
- `get_event_store()` — —
- `add_api_middlewares()` — —
- `get_openapi_config()` — —
- `api_response()` — —
- `is_valid_slug()` — —
- `is_valid_uuid()` — —
- `is_valid_email()` — —
- `slugify()` — —
- `validate_menu_name()` — —
- `validate_item_id()` — —
- `validate_route()` — —
- `create_app()` — —
- `create_api_router()` — —
- `ensure_cqrs_ready()` — —
- `get_session_for_table()` — —
- `check_memory_cache()` — —
- `check_database_connection()` — —
- `check_system_resources()` — —
- `check_environment()` — —
- `check_external_services()` — —
- `get_full_diagnostics()` — —
- `generate_recommendations()` — —
- `get_quick_diagnostics()` — —
- `start_protocol()` — —
- `get_protocol()` — —
- `get_protocol_document()` — —
- `execute_step()` — —
- `update_data()` — —
- `regenerate_document()` — —
- `pause_protocol()` — —
- `resume_protocol()` — —
- `complete_protocol()` — —
- `websocket_endpoint()` — —
- `sitemap_xml()` — —
- `robots_txt()` — —
- `list_users()` — —
- `select_user()` — —
- `login()` — —
- `get_session()` — —
- `logout()` — —
- `cql_proxy_info()` — —
- `cql_proxy_health()` — —
- `cql_quote()` — —
- `cql_unquote()` — —
- `cql_format_literal()` — —
- `cql_canonicalize()` — —
- `cql_normalize()` — —
- `cql_highlight()` — —
- `cql_parse()` — —
- `cql_serialize()` — —
- `cql_validate()` — —
- `cql_exec()` — —
- `cql_scenario_build()` — —
- `cql_capabilities()` — —
- `get_model()` — —
- `create_sgi()` — —
- `list_all_tables()` — —
- `list_rows()` — —
- `get_table_schema_alt()` — —
- `create_row()` — —
- `get_row()` — —
- `update_row()` — —
- `delete_row()` — —
- `parse_datetime()` — —
- `coerce_bool()` — —
- `coerce_column_types()` — —
- `remap_reserved_columns()` — —
- `normalize_datetime_fields()` — —
- `coerce_sgi_enabled()` — —
- `try_sgi_upsert()` — —
- `enrich_device_from_type()` — —
- `normalize_dsl_protocol_variables()` — —
- `normalize_template_variables()` — —
- `insert_with_retry()` — —
- `insert_template_variable()` — —
- `row_to_dict()` — —
- `parse_json_filters()` — —
- `apply_eq_filters()` — —
- `text_search()` — —
- `safe_execute_list_query()` — —
- `build_table_schema()` — —
- `get_all_schemas()` — —
- `get_table_schema()` — —
- `clear_schema_cache()` — —
- `template_proxy_get()` — —
- `template_proxy_post()` — —
- `template_proxy_patch()` — —
- `template_proxy_delete()` — —
- `list_activities()` — —
- `get_activity()` — —
- `create_activity()` — —
- `update_activity()` — —
- `delete_activity()` — —
- `list_assignments()` — —
- `get_assignment()` — —
- `create_assignment()` — —
- `update_assignment()` — —
- `delete_assignment()` — —
- `get_user()` — —
- `demo_users()` — —
- `demo_login()` — —
- `get_backups()` — —
- `get_backup_status()` — —
- `get_backup_health()` — —
- `create_backup_now()` — —
- `restore_from_backup()` — —
- `restore_latest_all()` — —
- `get_databases()` — —
- `get_namespace()` — —
- `get_key()` — —
- `set_key()` — —
- `delete_key()` — —
- `delete_namespace()` — —
- `batch_read()` — —
- `batch_ops()` — —
- `register_state_ws_handlers()` — —
- `list_config_sections()` — —
- `create_planfile_auto_task()` — —
- `get_config_section()` — —
- `update_config_section()` — —
- `delete_config_section()` — —
- `reset_config_section()` — —
- `get_system_config()` — —
- `get_hardware_status()` — —
- `validate_config()` — —
- `test_network_connection()` — —
- `ensure_main_nav_item()` — —
- `bridge_sidebar()` — —
- `replace_config()` — —
- `ensure_module_menu()` — —
- `ensure_config()` — —
- `get_config()` — —
- `upsert_item()` — —
- `get_or_404()` — —
- `delete_or_404()` — —
- `list_table()` — —
- `raise_on_failure()` — —
- `get_or_create()` — —
- `assert_not_exists()` — —
- `create_customer()` — —
- `get_customer()` — —
- `list_customers()` — —
- `update_customer()` — —
- `list_data_tables()` — —
- `list_configurations()` — —
- `proxy_data_requests()` — —
- `list_devices()` — —
- `get_device()` — —
- `create_device()` — —
- `update_device()` — —
- `delete_device()` — —
- `create_test_scenario()` — —
- `update_test_scenario()` — —
- `delete_test_scenario()` — —
- `list_workshops()` — —
- `list_protocols()` — —
- `list_table_rows()` — —
- `get_table_row()` — —
- `create_table_row()` — —
- `update_table_row()` — —
- `bulk_replace_table()` — —
- `delete_table_row()` — —
- `search_devices()` — —
- `retire_device()` — —
- `assign_device_to_customer()` — —
- `list_scripts()` — —
- `run_script()` — —
- `list_runs()` — —
- `get_run()` — —
- `get_run_logs()` — —
- `dsl_scan()` — —
- `dsl_standardize()` — —
- `dsl_sync_tables()` — —
- `dsl_import_xml()` — —
- `dsl_import_xml_by_name()` — —
- `dsl_verify_xml_import()` — —
- `list_objects()` — —
- `get_object()` — —
- `list_functions()` — —
- `get_function()` — —
- `list_params()` — —
- `get_param()` — —
- `list_units()` — —
- `get_dsl_logs()` — —
- `export_dsl_logs()` — —
- `clear_dsl_logs()` — —
- `execute_dsl_script()` — —
- `validate_def()` — —
- `validate_def_syntax()` — —
- `build_dsl_log_filters()` — —
- `get_dsl_logs_payload()` — —
- `export_dsl_logs_payload()` — —
- `clear_dsl_logs_payload()` — —
- `save_dynamic_page()` — —
- `get_dynamic_pages()` — —
- `delete_dynamic_page()` — —
- `list_events()` — —
- `list_streams()` — —
- `append_events()` — —
- `clear_stream()` — —
- `list_flags()` — —
- `list_flags_admin()` — —
- `list_categories()` — —
- `create_flag()` — —
- `update_flag()` — —
- `update_flag_by_key()` — —
- `delete_flag()` — —
- `reset_flags()` — —
- `list_icons()` — —
- `get_icon()` — —
- `get_icon_by_name()` — —
- `create_icon()` — —
- `update_icon()` — —
- `delete_icon()` — —
- `list_identification_endpoints()` — —
- `identify()` — —
- `get_history()` — —
- `clear_history()` — —
- `get_health()` — —
- `identify_shortcut()` — —
- `update_config()` — —
- `get_env()` — —
- `get_status()` — —
- `heartbeat()` — —
- `fleet_status()` — —
- `trigger_sync()` — —
- `get_kiosk_logs()` — —
- `restart_kiosk()` — —
- `list_backups()` — —
- `create_backup()` — —
- `get_library()` — —
- `update_library()` — —
- `add_library_item()` — —
- `delete_library_item()` — —
- `get_logs()` — —
- `get_log_stats()` — —
- `get_error_logs()` — —
- `ingest_frontend_logs()` — —
- `clear_logs()` — —
- `list_allowed_names()` — —
- `create_configuration()` — —
- `get_configuration()` — —
- `get_configuration_by_name()` — —
- `update_configuration()` — —
- `delete_configuration()` — —
- `create_or_update_item()` — —
- `delete_item()` — —
- `capture_screenshots()` — —
- `get_configuration_by_route()` — —
- `get_sidebar_structure()` — —
- `list_modules()` — —
- `broadcast_update()` — —
- `resolve_system_chrome()` — —
- `take_screenshots_playwright()` — —
- `take_screenshots_cli()` — —
- `save_screenshot()` — —
- `get_screenshots()` — —
- `get_screenshot_file()` — —
- `delete_screenshot()` — —
- `clear_screenshots()` — —
- `list_modules_admin()` — —
- `create_module()` — —
- `update_module()` — —
- `delete_module()` — —
- `reset_modules()` — —
- `get_navigation_options()` — —
- `list_navigation_contexts()` — —
- `list_all_navigation_options()` — —
- `create_navigation_option()` — —
- `update_navigation_option()` — —
- `delete_navigation_option()` — —
- `reset_navigation_options()` — —
- `list_redirects()` — —
- `list_aliases()` — —
- `get_redirect()` — —
- `create_redirect()` — —
- `update_redirect()` — —
- `delete_redirect()` — —
- `get_redirect_by_route()` — —
- `generate_report()` — —
- `list_reports()` — —
- `get_report()` — —
- `export_report()` — —
- `delete_report()` — —
- `list_roles()` — —
- `get_roles_meta()` — —
- `update_role_meta()` — —
- `create_role()` — —
- `update_role()` — —
- `delete_role()` — —
- `get_menu_access()` — —
- `set_menu_access()` — —
- `bulk_update_menu_access()` — —
- `seed_missing_access()` — —
- `delete_menu_access()` — —
- `ensure_default_roles()` — —
- `ensure_default_access()` — —
- `get_scenario_activities()` — —
- `add_activity_to_scenario()` — —
- `remove_activity_from_scenario()` — —
- `create_scenario()` — —
- `get_fallback_scenario()` — —
- `get_service_scenario_health()` — —
- `get_scenario()` — —
- `list_scenarios()` — —
- `update_scenario()` — —
- `delete_scenario()` — —
- `get_scenario_dsl()` — —
- `execute_scenario()` — —
- `get_execution_status()` — —
- `get_execution_logs()` — —
- `stop_execution()` — —
- `list_tables_v3()` — —
- `list_schema_labels_v3()` — —
- `get_schema_root_v3()` — —
- `get_table_schema_v3()` — —
- `refresh_schema_cache_v3()` — —
- `get_schema_version_v3()` — —
- `get_service_data_health()` — —
- `get_service_manager_health()` — —
- `get_service_reports_health()` — —
- `get_service_workshop_health()` — —
- `scan_frontend_modules()` — —
- `scan_database_tables()` — —
- `compare_connect_data_menu()` — —
- `add_missing_tables_to_menu()` — —
- `hide_extra_menu_items()` — —
- `get_test_kinds()` — —
- `update_test_kinds()` — —
- `add_test_kind()` — —
- `delete_test_kind()` — —
- `create_test_session()` — —
- `get_test_session()` — —
- `list_test_sessions()` — —
- `start_test_session()` — —
- `complete_test_session_step()` — —
- `get_test_session_history()` — —
- `pause_test_session()` — —
- `resume_test_session()` — —
- `cancel_test_session()` — —
- `complete_test_session()` — —
- `delete_by_id()` — —
- `reset_table()` — —
- `compare_versions()` — —
- `events_websocket()` — —
- `handle_cqrs_command()` — —
- `handle_cqrs_query()` — —
- `setup_websocket_handlers()` — —
- `get_connected_clients()` — —
- `get_websocket_stats()` — —
- `broadcast_event()` — —
- `event_record_to_dict()` — —
- `pytest_configure()` — —
- `api_url()` — —
- `api_client()` — —
- `utcnow()` — —
- `api_available()` — —
- `get_demo_status()` — —
- `seed_demo_data()` — —
- `reset_demo_data()` — —
- `get_demo_config()` — —
- `save_demo_config()` — —
- `detect_devices()` — —
- `configure_connect_template_backend_client()` — —
- `close_connect_template_backend_client()` — —
- `get_connect_template_backend_base_url()` — —
- `list_drafts()` — —
- `save_draft()` — —
- `publish_template()` — —
- `proxy()` — —
- `configure_cql_backend_client()` — —
- `close_cql_backend_client()` — —
- `get_cql_backend_base_url()` — —
- `capabilities()` — —
- `quote()` — —
- `format_literal()` — —
- `unquote()` — —
- `canonicalize()` — —
- `normalize()` — —
- `highlight()` — —
- `init_config_db()` — —
- `discover_domain_handlers()` — —
- `auto_register_domain()` — —
- `register_event_types_from_package()` — —
- `register_events_handlers()` — —
- `register_live_protocol_handlers()` — —
- `register_menu_handlers()` — —
- `register_sensors_handlers()` — —
- `register_identification_handlers()` — —
- `setup_cqrs_infrastructure()` — —
- `get_cqrs_container()` — —
- `handle_load_sgi()` — —
- `handle_save_sgi()` — —
- `handle_delete_sgi()` — —
- `handle_bulk_save_sgi()` — —
- `register_sgi_handlers()` — —
- `register_sgi_bus_handlers()` — —
- `register_testing_handlers()` — —
- `create_crud_router()` — —
- `get_scenario_db()` — —
- `get_auth_db()` — —
- `init_db()` — —
- `init_auth_db()` — —
- `init_scenario_db()` — —
- `get_event_bus()` — —
- `init_devtools_db()` — —
- `lifespan()` — —
- `query_search()` — —
- `root()` — —
- `init_menu_db()` — —
- `copy_old_menu_data_if_present()` — —
- `ensure_columns()` — —
- `ensure_main_navigation_structure()` — —
- `ensure_default_route_redirects()` — —
- `seed_module_menus()` — —
- `seed_menu_items_from_registry()` — —
- `get_first_column()` — —
- `update_existing_item()` — —
- `get_correlation_id()` — —
- `get_trace_context()` — —
- `setup_structured_logging()` — —
- `setup_nfo()` — —
- `apply_module_bindings()` — —
- `get_last_backup_time()` — —
- `needs_backup()` — —
- `cleanup_old_backups()` — —
- `run_backup()` — —
- `restore_backup()` — —
- `ensure_backup_dir()` — —
- `check_last_backup()` — —
- `pg_dump()` — —
- `pg_restore()` — —
- `export_tables()` — —
- `normalize_object_name()` — —
- `goals_from_cql()` — —
- `load_cql()` — —
- `scan_scenario_dsl_coverage()` — —
- `ensure_dsl_tables_updated()` — —
- `create_scenario_with_dsl()` — —
- `extract_from_dsl()` — —
- `extract_from_goals()` — —
- `parse_goals_from_dsl()` — —
- `dsl_from_goals()` — —
- `standardize_scenario_dsl_goals()` — —
- `sync_dsl_tables_from_scenarios()` — —
- `import_xml_file()` — —
- `import_xml_files()` — —
- `import_xml_by_name()` — —
- `verify_xml_import()` — —
- `get_repo_root()` — —
- `is_safe_xml_name()` — —
- `find_xml_files_by_name()` — —
- `normalize_text()` — —
- `read_xml_vars()` — —
- `generate_split_scenarios_from_vars()` — —
- `generate_dsl_from_xml_vars()` — —
- `goals_from_dsl_text()` — —
- `ensure_dsl_dictionary_tables()` — —
- `get_firmware_gateway()` — —
- `get_search_index_service()` — —
- `reset_search_index_service()` — —
- `gen_id()` — —
- `list_variables()` — —
- `get_schema()` — —
- `create_tables()` — —
- `create_client()` — —
- `test_placeholder()` — —
- `test_import()` — —
- `index_page()` — —
- `health_check()` — —
- `status()` — —
- `get_io_ports()` — —
- `test_probe_waveshare_modbus_detects_working_port()` — —
- `test_probe_waveshare_modbus_reports_adapter_only_when_no_response()` — —
- `test_extract_scenario_id_accepts_frontend_and_cli_keys()` — —
- `test_extract_inline_dsl_accepts_content_and_direct_fields()` — —
- `normalize_dsl_to_library()` — —
- `convert_goals_json_to_library()` — —
- `process_scenario_row()` — —
- `migrate_scenarios()` — —
- `process_scenario()` — —
- `verify_migration()` — —
- `nested_to_flat_route()` — —
- `normalize_column_id()` — —
- `get_module_from_route()` — —
- `seed_default_menu_structure()` — —
- `upsert_dsl_objects()` — —
- `upsert_dsl_functions()` — —
- `upsert_dsl_units()` — —
- `upsert_dsl_params()` — —
- `ensure_tables_and_seed()` — —
- `extract_entities_from_xml()` — —
- `build_scenario_content()` — —
- `link_object_functions_from_steps()` — —
- `upsert_scenario_row()` — —
- `nice_obj()` — —
- `get_db_path()` — —
- `load_dsl_data()` — —
- `migrate_dsl_to_def()` — —
- `rollback_migration()` — —
- `migrate_database()` — —
- `create_pg_schemas()` — —
- `backup_databases()` — —
- `check_table_exists()` — —
- `get_row_count()` — —
- `ensure_assignment()` — —
- `print_json()` — —
- `get_recent_scenarios()` — —
- `get_dsl_table_stats()` — —
- `check_table_existence()` — —
- `get_icons_from_db()` — —
- `migrate_file()` — —
- `migrate_frontend()` — —
- `generate_icon_mapping_file()` — —
- `parse_svg_definitions()` — —
- `categorize_icon()` — —
- `migrate_icons()` — —
- `load_icon_mapping()` — —
- `save_icon_mapping()` — —
- `is_unicode_icon()` — —
- `find_svg_mapping()` — —
- `list_menu_icons()` — —
- `preview_migration()` — —
- `apply_migration()` — —
- `list_current_mappings()` — —
- `edit_mappings()` — —
- `seed_encoder_config()` — —
- `get_target_column()` — —
- `validate_menu()` — —
- `fix_menu_columns()` — —
- `generate_def_code()` — —
- `get_logger()` — —
- `analyze_file()` — —
- `analyze_directory()` — —
- `test_setup_uses_module_registration_hooks()` — —
- `test_setup_is_idempotent_when_handlers_are_present()` — —
- `test_setup_continues_when_optional_module_registration_fails()` — —
- `client()` — —
- `event_loop()` — —
- `sample_identification_data()` — —
- `sample_device_data()` — —
- `spec()` — —
- `gateway()` — —
- `test_health_check()` — —
- `test_get_scenarios()` — —
- `test_get_scenario_by_id()` — —
- `test_get_scenario_not_found()` — —
- `test_register_dsl()` — —
- `test_start_execution()` — —
- `test_execute_step()` — —
- `test_send_command_start()` — —
- `test_send_command_unknown()` — —
- `test_get_projection()` — —
- `test_get_status()` — —
- `test_get_state()` — —
- `test_pause()` — —
- `test_resume()` — —
- `test_stop()` — —
- `test_retry_on_connect_error()` — —
- `test_spec_file_valid()` — —
- `service_data_contracts()` — —
- `test_service_data_slice_has_minimum_contracts()` — —
- `test_service_data_covers_both_modules()` — —
- `test_service_data_layers_python_exists()` — —
- `test_service_data_layers_frontend_exists()` — —
- `test_service_data_commands_have_transport()` — —
- `test_service_data_queries_have_transport()` — —
- `test_service_data_events_have_producers()` — —
- `test_service_data_filenames_match_content()` — —
- `health_client()` — —
- `test_health_endpoint_returns_200()` — —
- `test_health_endpoint_response_shape()` — —
- `test_health_endpoint_covers_slice_modules()` — —
- `test_health_endpoint_overall_reflects_worst_case()` — —
- `service_id_contracts()` — —
- `test_service_id_slice_has_minimum_contracts()` — —
- `test_service_id_covers_module()` — —
- `test_service_id_layers_python_exists()` — —
- `test_service_id_layers_frontend_exists_or_null()` — —
- `test_service_id_commands_have_transport()` — —
- `test_service_id_queries_have_transport()` — —
- `test_service_id_events_have_producers()` — —
- `test_service_id_filenames_match_content()` — —
- `test_service_id_health_returns_200()` — —
- `test_service_id_health_response_shape()` — —
- `test_service_id_health_checks_structure()` — —
- `test_service_id_health_aggregate_reflects_worst_case()` — —
- `manager_contracts()` — —
- `test_manager_slice_has_minimum_contracts()` — —
- `test_manager_contracts_have_discriminator()` — —
- `test_manager_contracts_have_kind()` — —
- `test_manager_contracts_filename_matches_discriminator()` — —
- `test_manager_contracts_python_layer_exists()` — —
- `test_manager_health_contract_exists()` — —
- `test_service_manager_health_returns_200()` — —
- `test_service_manager_health_response_shape()` — —
- `test_service_manager_health_checks_structure()` — —
- `test_service_manager_health_aggregate_reflects_worst_case()` — —
- `reports_contracts()` — —
- `test_reports_slice_has_minimum_contracts()` — —
- `test_reports_contracts_have_discriminator()` — —
- `test_reports_contracts_have_kind()` — —
- `test_reports_contracts_filename_matches_discriminator()` — —
- `test_reports_contracts_python_layer_exists()` — —
- `test_reports_health_contract_exists()` — —
- `test_service_reports_health_returns_200()` — —
- `test_service_reports_health_response_shape()` — —
- `test_service_reports_health_checks_structure()` — —
- `test_service_reports_health_aggregate_reflects_worst_case()` — —
- `workshop_contracts()` — —
- `test_workshop_slice_has_minimum_contracts()` — —
- `test_workshop_contracts_have_discriminator()` — —
- `test_workshop_contracts_have_kind()` — —
- `test_workshop_contracts_filename_matches_discriminator()` — —
- `test_workshop_contracts_python_layer_exists()` — —
- `test_workshop_health_contract_exists()` — —
- `test_service_workshop_health_returns_200()` — —
- `test_service_workshop_health_response_shape()` — —
- `test_service_workshop_health_checks_structure()` — —
- `test_service_workshop_health_aggregate_reflects_worst_case()` — —
- `mock_db_session()` — —
- `test_v3_protocols_accept_large_limit_over_http()` — —
- `cqrs_container()` — —
- `event_store_with_mock_db()` — —
- `test_devtools_dsl_routes_delegate_to_shared_services()` — —
- `test_dsl_execute_endpoint_returns_deprecation_payload()` — —
- `test_validate_def_endpoint_returns_valid_for_correct_code()` — —
- `test_validate_def_endpoint_returns_errors_for_invalid_code()` — —
- `test_validate_def_endpoint_returns_errors_for_empty_code()` — —
- `test_dsl_logs_export_has_canonical_logs_route_and_dsl_alias()` — —
- `test_health_check_quiet()` — —
- `test_execution_lifecycle()` — —
- `test_send_command()` — —
- `firmware_sim_server()` — —
- `test_start_live_protocol_dispatches_to_real_firmware_sim()` — —
- `test_live_protocol_api_flow_reuses_firmware_execution_id()` — —
- `test_live_protocol_api_lifecycle_delegates_to_firmware()` — —
- `fresh_container()` — —
- `test_menu_handlers_are_registered_in_buses()` — —
- `test_menu_commands_and_queries_dispatch_through_bus()` — —
- `test_scenario_endpoints_bridge_legacy_columns_from_storage()` — —
- `test_create_and_update_scenario_keep_library_and_legacy_columns_in_sync()` — —
- `test_sgi_handlers_are_registered_in_buses()` — —
- `test_sgi_commands_and_query_dispatch_through_bus()` — —
- `test_customer_devices_projection_groups_by_customer()` — —
- `test_customer_devices_projection_empty_list()` — —
- `test_device_list_projection_builds_latest_state()` — —
- `test_device_list_projection_handles_retired_device()` — —
- `test_device_list_projection_empty_events()` — —
- `ensure_user()` — —
- `test_session_no_user_returns_none()` — —
- `test_session_missing_id_is_recovered_and_persisted()` — —
- `test_invalid_session_without_username_is_cleared()` — —
- `test_id_is_always_string()` — —
- `test_backups_list_delegates_to_service()` — —
- `test_databases_endpoint_returns_configured_pg_labels()` — —
- `test_create_backup_returns_service_count()` — —
- `test_restore_backup_maps_service_errors()` — —
- `test_backup_health_endpoint()` — —
- `test_backup_status_pg_mode()` — —
- `test_restore_latest_endpoint_dry_run()` — —
- `setup_module()` — —
- `test_v3_config_settings_roundtrip()` — —
- `test_v3_config_reset_defaults()` — —
- `test_v3_config_hardware_status_alias()` — —
- `test_v3_protocols_accept_large_limit()` — —
- `test_v3_data_protocols_prefers_proxy_list()` — —
- `test_v3_data_protocols_falls_back_list_on_proxy_502()` — —
- `test_v3_data_protocols_prefers_proxy_get()` — —
- `test_v3_data_protocols_falls_back_get_on_proxy_502()` — —
- `test_v3_data_customers_create_and_patch_use_domain_surface()` — —
- `test_v3_data_devices_create_patch_and_delete_use_domain_surface()` — —
- `test_v3_data_test_scenarios_legacy_surface_uses_scenario_owner()` — —
- `test_v3_data_test_scenarios_prefers_proxy_path()` — —
- `test_v3_data_test_scenarios_falls_back_to_legacy_on_proxy_502()` — —
- `test_v3_data_sgi_create_and_load_for_type_device_context()` — —
- `test_v3_data_sgi_update_existing_type_row_via_patch()` — —
- `test_v3_menu_list_and_main_navigation()` — —
- `test_v3_schema_and_labels_do_not_500()` — —
- `test_v1_auth_session_does_not_500()` — —
- `test_menu_v2_v3_endpoints_do_not_500()` — —
- `backend_root()` — —
- `test_cqrs_script_exists()` — —
- `test_generated_menu_contracts_import_and_instantiation()` — —
- `test_append_frontend_logs_persists_browser_entries()` — —
- `teardown_module()` — —
- `test_upsert_main_navigation_connect_manager()` — —
- `test_create_connect_manager_config_and_by_route()` — —
- `test_get_menu_by_route_exact_match_flag_controls_prefix_resolution()` — —
- `test_module_menu_alias_resolves_to_base_configuration()` — —
- `test_create_configuration_persists_inactive_state()` — —
- `test_create_configuration_persists_item_active_and_visible_flags()` — —
- `test_update_configuration_persists_route_mappings()` — —
- `test_query_handler_resolves_menu_alias_without_warning()` — —
- `test_logging_after_operations()` — —
- `test_update_main_navigation_logs()` — —
- `db_session()` — —
- `clean_menu_config()` — —
- `test_update_configuration_passes_route_mappings_to_command()` — —
- `test_update_handler_applies_route_mappings()` — —
- `test_ensure_default_roles_skips_access_seed_by_default()` — —
- `test_ensure_default_roles_can_seed_access_when_requested()` — —
- `isolated_search_db()` — —
- `test_queries_search_returns_local_index_results()` — —
- `test_queries_search_returns_empty_when_service_unavailable()` — —
- `test_backend_version_endpoints_expose_version()` — —
- `test_version_compare_reports_match()` — —
- `test_version_compare_reports_mismatch()` — —
- `test_send_to_client_treats_normal_close_as_non_error()` — —
- `test_run_seeds_accepts_canonical_seed_history_schema()` — —
- `test_ensure_seed_history_migrates_legacy_runtime_schema()` — —
- `test_goals_from_cql_uses_oqlos_ast_path_for_basic_steps()` — —
- `test_goals_from_cql_falls_back_to_legacy_when_parse_cql_raises()` — —
- `test_data_list_customers_proxies_to_connect_data()` — —
- `test_data_get_device_proxies_to_connect_data()` — —
- `test_data_create_customer_proxies_to_connect_data()` — —
- `test_data_delete_device_proxies_to_connect_data()` — —
- `test_data_list_protocols_proxies_to_connect_protocol()` — —
- `test_data_list_protocols_filter_id_uses_get_details()` — —
- `test_data_get_protocol_maps_upstream_not_found()` — —
- `test_export_dsl_emits_replayable_api_lines_and_comments_unknown_entries()` — —
- `test_scan_scenario_dsl_coverage_counts_missing_and_invalid_rows()` — —
- `test_standardize_scenario_dsl_goals_updates_missing_fields()` — —
- `test_standardize_scenario_dsl_goals_builds_goals_from_real_task_style_dsl()` — —
- `test_sync_dsl_tables_from_scenarios_returns_counts_from_collected_entities()` — —
- `test_sync_dsl_tables_from_scenarios_bootstraps_missing_dictionary_tables()` — —
- `test_generate_dsl_from_xml_vars_emits_goal_tasks_and_conditions()` — —
- `test_generate_dsl_from_xml_vars_emits_wait_and_operator_confirmation()` — —
- `test_generate_dsl_from_xml_vars_maps_outputs_to_set_without_task_keyword()` — —
- `test_generate_dsl_from_xml_vars_maps_compressor_to_set_with_lpm()` — —
- `test_generate_split_scenarios_from_vars_splits_per_transaction()` — —
- `test_create_scenario_with_dsl_supports_legacy_schema_without_duration()` — —
- `test_import_xml_file_returns_single_file_import_details()` — —
- `test_start_handler_calls_firmware_and_persists_execution_id()` — —
- `test_execute_step_uses_firmware_execution_id_from_protocol()` — —
- `test_firmware_gateway_retries_connect_errors_then_succeeds()` — —
- `test_firmware_health_check_is_quiet_for_unreachable_service()` — —
- `test_firmware_gateway_get_scenario_uses_detail_endpoint()` — —
- `test_firmware_gateway_register_dsl_uses_scenarios_endpoint()` — —
- `test_get_firmware_gateway_uses_settings_base_url()` — —
- `test_protocol_lifecycle_handlers_delegate_to_firmware()` — —
- `test_ensure_success_invalid_json_returns_bad_gateway()` — —
- `test_ensure_success_not_found_domain_error()` — —
- `test_ensure_success_domain_error_maps_to_bad_request()` — —
- `test_start_protocol_forwards_payload()` — —
- `test_get_protocol_document_forwards_format_query()` — —
- `test_list_protocols_forwards_filters_and_pagination()` — —
- `test_pause_protocol_forwards_reason()` — —
- `test_resume_protocol_forwards_endpoint()` — —
- `test_complete_protocol_forwards_payload()` — —
- `test_run_column_migrations_adds_legacy_scenario_columns_even_for_partial_db()` — —
- `test_scenario_session_uses_main_engine_when_urls_match()` — —
- `test_goals_helpers_accept_legacy_goals_column_payload()` — —
- `test_effective_library_bridges_legacy_content_goals_and_def()` — —
- `test_normalize_sqlite_url_resolves_backend_relative_paths()` — —
- `test_raise_for_upstream_maps_not_found_domain_error()` — —
- `test_raise_for_upstream_invalid_json_returns_bad_gateway()` — —
- `test_delete_scenario_forwards_reason_to_connect_backend()` — —
- `test_delete_scenario_without_body_sends_null_json()` — —
- `test_get_scenario_dsl_maps_upstream_payload()` — —
- `test_execute_scenario_forwards_mode_and_speed()` — —
- `test_get_execution_logs_forwards_limit_param()` — —
- `test_get_fallback_scenario_returns_default_for_unknown_device()` — —
- `test_get_service_scenario_health_aggregates_upstream_checks()` — —
- `test_map_proxy_error_status_not_found()` — —
- `test_map_proxy_error_status_conflict_states()` — —
- `test_map_proxy_error_status_default_bad_request()` — —
- `test_ensure_success_raises_for_invalid_json()` — —
- `test_ensure_success_maps_domain_error()` — —
- `test_ensure_success_returns_payload()` — —
- `test_complete_test_session_step_proxies_and_maps_response()` — —
- `test_complete_test_session_step_raises_not_found()` — —
- `setup_test_databases()` — —
- `data_health()` — —
- `test_health_endpoint_reports_service_metadata()` — —
- `test_customers_endpoint_envelope()` — —
- `test_create_device_returns_legacy_data_shape()` — —
- `encoder_status()` — —
- `encoder_debug()` — —
- `encoder_activate()` — —
- `encoder_deactivate()` — —
- `encoder_scroll()` — —
- `encoder_click()` — —
- `encoder_cancel()` — —
- `encoder_focus()` — —
- `encoder_page_next()` — —
- `encoder_page_prev()` — —
- `dashboard_page()` — —
- `dashboard_action_page()` — —
- `files_page()` — —
- `files_with_path_page()` — —
- `files_with_table_page()` — —
- `files_with_action_page()` — —
- `docs_page()` — —
- `client_js()` — —
- `c2004_test_page()` — —
- `anyio_backend()` — —
- `test_index_page()` — —
- `test_dashboard_page()` — —
- `test_encoder_status_no_browser()` — —
- `test_oql_files_endpoint()` — —
- `test_hub_is_initialized()` — —
- `cprint()` — —
- `api_call()` — —
- `get()` — —
- `post()` — —
- `cmd()` — —
- `cmd_status()` — —
- `cmd_activate()` — —
- `cmd_deactivate()` — —
- `cmd_scroll()` — —
- `cmd_click()` — —
- `cmd_cancel()` — —
- `cmd_focus()` — —
- `cmd_page_next()` — —
- `cmd_page_prev()` — —
- `cmd_run()` — —
- `cmd_help()` — —
- `print_status()` — —
- `run_oql_file()` — —
- `shell()` — —
- `api_get()` — —
- `api_post()` — —
- `check_browser_connected()` — —
- `test_files_page()` — —
- `test_docs_page()` — —
- `test_api_docs_page()` — —
- `test_client_js()` — —
- `test_encoder_activate_no_browser()` — —
- `test_encoder_scroll_no_browser()` — —
- `test_encoder_click_no_browser()` — —
- `test_encoder_cancel_no_browser()` — —
- `test_encoder_focus_no_browser()` — —
- `test_encoder_page_next_no_browser()` — —
- `test_encoder_page_prev_no_browser()` — —
- `test_oql_list_files()` — —
- `test_oql_read_file()` — —
- `test_oql_read_file_legacy_c2004_path()` — —
- `test_oql_read_file_not_found()` — —
- `test_oql_read_file_path_traversal()` — —
- `test_oql_run_line_comment()` — —
- `test_oql_run_line_wait()` — —
- `test_oql_run_line_set()` — —
- `test_oql_run_line_unknown()` — —
- `test_oql_run_line_encoder_scroll()` — —
- `test_oql_run_line_encoder_focus()` — —
- `test_hub_no_clients()` — —
- `test_hub_disconnect_nonexistent()` — —
- `history()` — —
- `id_health()` — —
- `test_module_health_endpoint_reports_connect_id_module()` — —
- `repo()` — —
- `handler()` — —
- `test_identify_user_rfid()` — —
- `test_identify_invalid_rfid()` — —
- `test_identify_qr()` — —
- `test_clear_history()` — —
- `test_identification_is_relayed_to_subscriber_inbox()` — —
- `create_schedule()` — —
- `list_schedules()` — —
- `manager_health()` — —
- `test_module_health_endpoint_reports_connect_manager_module()` — —
- `test_capabilities_lists_expected_endpoints()` — —
- `create_handler()` — —
- `update_handler()` — —
- `schedule_handler()` — —
- `delete_handler()` — —
- `list_handler()` — —
- `get_handler()` — —
- `test_create_assignment()` — —
- `test_update_assignment_status()` — —
- `test_update_assignment_status_not_found()` — —
- `test_schedule_periodic_test()` — —
- `test_delete_assignment()` — —
- `test_delete_assignment_not_found()` — —
- `test_list_assignments_query()` — —
- `test_list_assignments_filtered_by_status()` — —
- `test_get_assignment_query()` — —
- `test_get_assignment_query_not_found()` — —
- `test_inbox_event_triggers_identify_to_assignment_saga()` — —
- `saga_setup()` — —
- `test_saga_creates_assignment_for_user_with_hint()` — —
- `test_saga_skips_event_without_hint()` — —
- `test_saga_ignores_non_user_identifications()` — —
- `test_saga_is_idempotent_on_correlation_id()` — —
- `test_identify_to_assignment_saga_creates_assignment_for_user()` — —
- `test_identify_to_assignment_saga_ignores_non_user()` — —
- `test_identify_to_assignment_saga_is_idempotent_by_correlation_id()` — —
- `protocol_health()` — —
- `test_protocol_lifecycle_through_api()` — —
- `test_list_protocols_filters_by_status()` — —
- `publish_scenario()` — —
- `add_activity()` — —
- `hardware_health_proxy()` — —
- `hardware_identify_proxy()` — —
- `hardware_proxy_info()` — —
- `hardware_diagnostic_command()` — —
- `scenario_health()` — —
- `test_module_health_endpoint_reports_connect_scenario_module()` — —
- `test_scenario_survives_new_handler()` — —
- `test_publish_and_list_scenarios()` — —
- `test_activities_persisted()` — —
- `publish_handler()` — —
- `add_activity_handler()` — —
- `test_create_scenario()` — —
- `test_update_scenario()` — —
- `test_publish_and_add_activity()` — —
- `test_update_scenario_not_found()` — —
- `health_full()` — —
- `bridge_scenario()` — —
- `parse_endpoint()` — —
- `serialize_endpoint()` — —
- `validate_endpoint()` — —
- `exec_endpoint()` — —
- `scenario_build_endpoint()` — —
- `xsd_endpoint()` — —
- `json_schema_endpoint()` — —
- `validate_ast_endpoint()` — —
- `dsl_to_xml_endpoint()` — —
- `ast_to_xml_endpoint()` — —
- `xml_to_ast_endpoint()` — —
- `validate_dsl_text_endpoint()` — —
- `xml_migrate_endpoint()` — —
- `xml_split_endpoint()` — —
- `list_scenario_files()` — —
- `get_scenario_file()` — —
- `save_scenario_file()` — —
- `configure_maskservice_client()` — —
- `close_maskservice_client()` — —
- `get_maskservice_base_url()` — —
- `fetch_scenario()` — —
- `normalize_dsl()` — —
- `normalize_quoted_dsl_line()` — —
- `render_legacy_task_as_dsl_lines()` — —
- `dsl_from_scenario_content()` — —
- `goals_from_content()` — —
- `execute_ast()` — —
- `execute_dsl()` — —
- `highlight_line()` — —
- `highlight_dsl()` — —
- `migrate_legacy_xml_to_dsl()` — —
- `split_legacy_xml_to_scenarios()` — —
- `parse_dsl()` — —
- `normalize_dsl_line_quotes()` — —
- `normalize_dsl_text_quotes()` — —
- `format_dsl_literal()` — —
- `quote_dsl_value()` — —
- `canonicalize_dsl_line_quotes()` — —
- `canonicalize_dsl_quotes()` — —
- `read_quoted_token()` — —
- `get_json_schema()` — —
- `validate_ast()` — —
- `ast_to_dsl_text()` — —
- `normalize_dsl_text()` — —
- `validate_dsl_text()` — —
- `validate_dsl_format()` — —
- `ast_to_xml()` — —
- `dsl_to_xml()` — —
- `xml_to_ast()` — —
- `test_health()` — —
- `test_capabilities_lists_implemented_and_no_stubs()` — —
- `test_quote()` — —
- `test_quote_apostrophe_uses_double()` — —
- `test_unquote()` — —
- `test_canonicalize()` — —
- `test_highlight_basic_keywords_are_wrapped()` — —
- `test_parse_returns_ast()` — —
- `test_validate_returns_result()` — —
- `test_exec_returns_plan()` — —
- `test_serialize_returns_text()` — —
- `test_scenario_build_returns_dsl()` — —
- `test_xsd_endpoint_returns_static_definition()` — —
- `test_json_schema_endpoint_matches_root_shape()` — —
- `test_validate_ast_accepts_minimal_valid_ast()` — —
- `test_validate_ast_rejects_missing_scenario()` — —
- `test_validate_ast_rejects_empty_goals_and_funcs()` — —
- `test_ast_to_xml_returns_indented_xml()` — —
- `test_dsl_to_xml_round_trips_via_xml_to_ast()` — —
- `test_xml_to_ast_rejects_missing_root()` — —
- `test_xml_to_ast_rejects_invalid_xml()` — —
- `test_dsl_to_xml_returns_errors_on_bad_input()` — —
- `test_validate_dsl_text_succeeds_for_valid_dsl()` — —
- `test_validate_dsl_text_collects_parse_errors()` — —
- `test_xml_migrate_handles_native_xml()` — —
- `test_xml_migrate_handles_legacy_report()` — —
- `test_xml_migrate_returns_errors_on_unrelated_xml()` — —
- `test_xml_split_returns_one_scenario_per_transaction()` — —
- `test_xml_split_returns_empty_on_native_xml()` — —
- `test_health_full_reports_unreachable_when_maskservice_down()` — —
- `test_health_full_reports_ok_when_maskservice_returns_200()` — —
- `test_bridge_scenario_returns_upstream_record()` — —
- `test_bridge_scenario_returns_404_when_upstream_404()` — —
- `test_bridge_scenario_returns_503_when_upstream_unreachable()` — —
- `test_exec_simple_dsl()` — —
- `test_exec_with_condition()` — —
- `test_exec_with_log()` — —
- `test_exec_returns_ast()` — —
- `test_parse_simple_scenario()` — —
- `test_parse_with_condition()` — —
- `test_parse_with_task()` — —
- `test_parse_empty_returns_error()` — —
- `test_parse_with_func()` — —
- `test_parse_with_wait()` — —
- `test_format_dsl_literal_defaults_to_single_quotes()` — —
- `test_format_dsl_literal_falls_back_to_double_quotes_with_apostrophe()` — —
- `test_quote_dsl_value_handles_none()` — —
- `test_normalize_line_single_to_double()` — —
- `test_normalize_text_multiline()` — —
- `test_canonicalize_line_double_to_single()` — —
- `test_canonicalize_preserves_double_quotes_for_values_with_apostrophes()` — —
- `test_canonicalize_text_multiline()` — —
- `test_read_quoted_token_single()` — —
- `test_read_quoted_token_double()` — —
- `test_read_quoted_token_bare()` — —
- `test_build_dsl_from_test_scenario()` — —
- `test_build_goals_from_test_scenario()` — —
- `test_build_dsl_from_generic_scenario()` — —
- `test_build_dsl_with_duration()` — —
- `test_serialize_simple_ast()` — —
- `test_serialize_with_set_step()` — —
- `test_serialize_with_if_condition()` — —
- `test_validate_valid_dsl()` — —
- `test_validate_detects_missing_scenario()` — —
- `test_validate_detects_placeholder_star()` — —
- `test_validate_returns_fixed_text()` — —
- `test_validate_detects_missing_get()` — —
- `create_template()` — —
- `update_template()` — —
- `create_draft()` — —
- `get_template()` — —
- `list_templates()` — —
- `get_service()` — —
- `delete_template()` — —
- `list_json_data()` — —
- `create_json_data()` — —
- `update_json_data()` — —
- `delete_json_data()` — —
- `preview_template()` — —
- `test_preview_endpoint_uses_service_override()` — —
- `test_template_survives_new_handler()` — —
- `test_publish_and_filter_templates()` — —
- `test_create_template()` — —
- `test_update_template()` — —
- `test_publish_template()` — —
- `test_update_nonexistent()` — —
- `test_save_draft_creates_new_record_without_stable_lookup_keys()` — —
- `test_save_draft_updates_existing_record_for_matching_preset()` — —
- `test_publish_template_increments_version_and_marks_published()` — —
- `test_render_preview_resolves_svg_paths_and_clears_unknown_placeholders()` — —
- `start_session()` — —
- `complete_step()` — —
- `finalize_session()` — —
- `list_sessions()` — —
- `get_session_history()` — —
- `pause_session()` — —
- `resume_session()` — —
- `cancel_session()` — —
- `test_module_health_endpoint_reports_connect_test_module()` — —
- `start_handler()` — —
- `step_handler()` — —
- `finalize_handler()` — —
- `test_start_session()` — —
- `test_complete_step()` — —
- `test_finalize_session()` — —
- `test_step_without_session()` — —
- `generate_individual()` — —
- `generate_sprite()` — —
- `extract_symbols()` — —
- `count_symbols()` — —
- `add_MenuCommandServiceServicer_to_server()` — —
- `add_MenuQueryServiceServicer_to_server()` — —
- `deploy()` — —
- `curl_json()` — —
- `curl_status()` — —
- `curl_text()` — —
- `ssh_cmd()` — —
- `check_reachable()` — —
- `check_backend_health()` — —
- `check_firmware_health()` — —
- `check_frontend_html()` — —
- `check_config_js()` — —
- `check_api_menu()` — —
- `check_api_config_sections()` — —
- `check_api_auth_session()` — —
- `check_api_state()` — —
- `check_db_counts()` — —
- `check_proxy_passthrough()` — —
- `check_proxy_firmware()` — —
- `check_js_bundles()` — —
- `check_kiosk_browser()` — —
- `check_screen_resolution()` — —
- `check_services_running()` — —
- `check_data_sync()` — —
- `run_checks()` — —
- `compare_targets()` — —
- `create_health_router()` — —
- `build_version_payload()` — —
- `create_version_router()` — —
- `verify_password()` — —
- `create_command_router()` — —
- `create_query_router()` — —
- `result_to_response()` — —
- `create_inbox_router()` — —
- `setup_cqrs()` — —
- `reset_cqrs_setup()` — —
- `build_alembic_env_main()` — —
- `query_events()` — —
- `append_events_to_stream()` — —
- `list_event_streams()` — —
- `clear_event_stream()` — —
- `create_logs_router()` — —
- `resolve_logs_db_path()` — —
- `build_logs_query_service()` — —
- `item_to_command()` — —
- `column_to_command()` — —
- `columns_to_commands()` — —
- `route_mappings_to_commands()` — —
- `delete_menu_item_from_db()` — —
- `list_menu_modules()` — —
- `broadcast_menu_update()` — —
- `define_menu_models()` — —
- `create_nfo_setup()` — —
- `clean_version()` — —
- `resolve_release_version()` — —
- `get_pressure_service()` — —
- `create_sensors_router()` — —
- `parse_args()` — —
- `is_ignored()` — —
- `iter_python_files()` — —
- `is_within()` — —
- `module_parts_for()` — —
- `build_workspace_index()` — —
- `pick_best()` — —
- `resolve_absolute()` — —
- `resolve_relative()` — —
- `classify_external()` — —
- `edge_bucket()` — —
- `build_yaml()` — —
- `yaml_scalar()` — —
- `to_module_label()` — —
- `normalize_edges()` — —
- `format_top_list()` — —
- `render_toon()` — —
- `write_payloads()` — —
- `analyze()` — —
- `log_error()` — —
- `log_success()` — —
- `log_warning()` — —
- `log_info()` — —
- `log_header()` — —
- `build_import_graph()` — —
- `find_circular_imports()` — —
- `analyze_module_coupling()` — —
- `calculate_import_depth()` — —
- `generate_dot_graph()` — —
- `find_all_ts_files()` — —
- `get_modules()` — —
- `get_pages()` — —
- `count_references()` — —
- `find_external_apis()` — —
- `find_large_files()` — —
- `analyze_module_usage()` — —
- `analyze_page_usage()` — —
- `find_similar_files()` — —
- `find_refactor_candidates()` — —
- `backup_database()` — —
- `get_tables()` — —
- `drop_table()` — —
- `consolidate_scenario_db()` — —
- `consolidate_root_identification_db()` — —
- `consolidate_app_db()` — —
- `update_backend_db_config()` — —
- `convert_dsl_syntax()` — —
- `find_duplicate_code_blocks()` — —
- `find_unused_imports_ts()` — —
- `find_unused_imports_py()` — —
- `find_todos()` — —
- `find_console_logs()` — —
- `find_all_databases()` — —
- `get_table_info()` — —
- `compare_schemas()` — —
- `find_duplicates()` — —
- `analyze_duplicates()` — —
- `generate_migration_sql()` — —
- `find_all_exports_ts()` — —
- `find_all_imports_ts()` — —
- `find_unused_files()` — —
- `find_empty_functions()` — —
- `should_skip()` — —
- `find_python_models()` — —
- `find_typescript_models()` — —
- `scan_directory()` — —
- `print_report()` — —
- `should_skip_name()` — —
- `is_component_specific_name()` — —
- `is_component_specific_path()` — —
- `classify_type()` — —
- `should_skip_path()` — —
- `find_enums()` — —
- `find_dataclasses()` — —
- `find_interfaces()` — —
- `scan_project()` — —
- `find_shared_interfaces()` — —
- `print_priority_report()` — —
- `generate_revision_id()` — —
- `generate_migration_file()` — —
- `cmd_add_column()` — —
- `cmd_drop_column()` — —
- `cmd_rename_column()` — —
- `cmd_add_table()` — —
- `cmd_drop_table()` — —
- `cmd_add_index()` — —
- `cmd_add_fk()` — —
- `save_migration()` — —
- `get_file_category()` — —
- `is_test_file()` — —
- `is_cqrs_file()` — —
- `count_lines()` — —
- `detect_layer()` — —
- `infer_source_from_test()` — —
- `extract_imports()` — —
- `resolve_import_path()` — —
- `build_dependency_graph()` — —
- `find_cycles()` — —
- `calculate_coupling_metrics()` — —
- `detect_module_boundaries()` — —
- `extract_module_name()` — —
- `analyze_typescript_deps()` — —
- `find_singleton_files()` — —
- `find_global_instances()` — —
- `find_shared_state()` — —
- `analyze_singleton_dependencies()` — —
- `calculate_singleton_metrics()` — —
- `generate_di_recommendations()` — —
- `read_file_safe()` — —
- `extract_module_from_path()` — —
- `extract_exports()` — —
- `extract_singleton_deps()` — —
- `is_at_module_level()` — —
- `analyze_singletons()` — —
- `find_cqrs_directories()` — —
- `analyze_cqrs_completeness()` — —
- `find_modules_without_cqrs()` — —
- `extract_event_handlers()` — —
- `analyze_handler_coverage()` — —
- `find_all_events()` — —
- `analyze_cqrs_consistency()` — —
- `calculate_consistency_score()` — —
- `generate_cqrs_recommendations()` — —
- `extract_parent_module()` — —
- `group_by_module()` — —
- `analyze_cqrs()` — —
- `analyze_syntactic_debt()` — —
- `analyze_structural_debt()` — —
- `analyze_semantic_debt()` — —
- `analyze_architectural_debt()` — —
- `decompose_file()` — —
- `decompose_project()` — —
- `load_json_safe()` — —
- `generate_executive_summary()` — —
- `generate_task_list()` — —
- `generate_llm_prompts()` — —
- `generate_verification_checklist()` — —
- `generate_final_report()` — —
- `validate_dsl()` — —
- `parse_summary()` — —
- `generate_html_report()` — —
- `generate_markdown_report()` — —
- `extract_backend_endpoints()` — —
- `extract_frontend_api_calls()` — —
- `normalize_path()` — —
- `find_deprecated_patterns()` — —
- `validate_endpoints()` — —
- `extract_tables_from_models()` — —
- `extract_tables_from_api()` — —
- `get_db_tables()` — —
- `generate_create_table_sql()` — —
- `validate_schema()` — —
- `parse_sqlalchemy_models()` — —
- `get_db_schema()` — —
- `normalize_type()` — —
- `compare_types()` — —
- `validate_models()` — —
- `extract_typescript_interfaces()` — —
- `parse_ts_fields()` — —
- `extract_python_models()` — —
- `parse_python_fields()` — —
- `normalize_type_name()` — —
- `find_matching_types()` — —
- `validate_types()` — —
- `strip_nfo_fallback_blocks()` — —
- `parse_auto_log_modules()` — —
- `get_module_name_for_file()` — —
- `check_nfo_in_requirements()` — —
- `check_setup_nfo_in_main()` — —
- `audit_file()` — —
- `collect_py_files()` — —
- `audit_service()` — —
- `c()` — —
- `print_separator()` — —
- `print_header()` — —
- `print_service_report()` — —
- `print_summary()` — —
- `output_json()` — —
- `bootstrap()` — —
- `find_emoji_in_line()` — —
- `scan_app_files()` — —
- `is_emoji()` — —
- `find_emoji_in_file()` — —
- `scan_frontend_directory()` — —
- `parse_line()` — —
- `parse_script()` — —
- `find_svg_tokens()` — —
- `build_output()` — —
- `profile_get_section()` — —
- `profile_health()` — —
- `profile_auth_session()` — —
- `profile_auth_demo_users()` — —
- `profile_menu_configurations()` — —
- `profile_menu_sidebar()` — —
- `profile_menu_modules()` — —
- `profile_connect_menu_api()` — —
- `profile_config_sections()` — —
- `profile_config_system()` — —
- `profile_data_tables()` — —
- `profile_scenarios()` — —
- `profile_reports()` — —
- `profile_template_json()` — —
- `profile_devtools_scripts()` — —
- `profile_version()` — —
- `profile_diagnostics()` — —
- `profile_workshops()` — —
- `profile_test_sessions()` — —
- `profile_assignments()` — —
- `profile_activities()` — —
- `profile_startup_sequence()` — —
- `profile_navigation_sequence()` — —
- `profile_concurrent_startup()` — —
- `migrate_table()` — —
- `migrate()` — —
- `normalize_icon_token()` — —
- `collect_changes()` — —
- `apply_changes()` — —
- `apply_changes_limited()` — —
- `patch_history_patterns()` — —
- `patch_url_fallback()` — —
- `patch_apply_step()` — —
- `compute_scores()` — —
- `format_report()` — —
- `perf_recommendations()` — —
- `code_quality_recommendations()` — —
- `run_backend_tests()` — —
- `run_api_tests()` — —
- `run_frontend_tests()` — —
- `run_connect_data_tests()` — —
- `run_performance_profile()` — —
- `run_code_quality()` — —
- `run_command()` — —
- `refactor_file()` — —
- `remove_inline_styles()` — —
- `process_file()` — —
- `log()` — —
- `load_config()` — —
- `save_config()` — —
- `get_hourly_activity()` — —
- `find_optimal_restart_hour()` — —
- `compute_optimal_restart_time()` — —
- `load_schedule()` — —
- `save_schedule()` — —
- `compute_next_restart()` — —
- `schedule_restart()` — —
- `send_notification_to_frontend()` — —
- `notify_restart()` — —
- `is_test_in_progress()` — —
- `was_cancel_requested()` — —
- `cancel_restart()` — —
- `execute_restart()` — —
- `check_startup_restart()` — —
- `daemon_loop()` — —
- `show_status()` — —
- `write_if_missing()` — —
- `package_json()` — —
- `tsconfig_json()` — —
- `push_to_api()` — —
- `generate_dsl()` — —
- `generate_cql()` — —
- `generate_goals_json()` — —
- `parse_xml()` — —
- `is_pump_output()` — —
- `is_compressor_output()` — —
- `normalize_output_name()` — —
- `normalize_flow_value()` — —
- `normalize_set_value()` — —
- `pbkdf2_hash()` — —
- `now_iso()` — —
- `seed_users()` — —
- `get_db_url()` — —
- `run_db_seeds()` — —
- `run_alembic_upgrade()` — —
- `list_all_seeds()` — —
- `get_admin_role_id()` — —
- `get_all_routes()` — —
- `set_admin_full_access()` — —
- `verify_admin_access()` — —
- `get_system_info()` — —
- `get_test_stats()` — —
- `get_restart_stats()` — —
- `record_restart()` — —
- `collect_daily_stats()` — —
- `upload_stats()` — —
- `upload_pending_stats()` — —
- `copy_to_pendrive()` — —
- `get_identification_service()` — —
- `patch_row()` — —
- `get_table_columns()` — —
- `startup_event()` — —
- `shutdown_event()` — —
- `ensure_demo_schema()` — —
- `list_tables()` — —
- `list_columns()` — —
- `get_rows()` — —
- `create_protocol()` — —
- `get_protocols()` — —
- `update_protocol()` — —
- `save_protocol()` — —
- `delete_protocol()` — —
- `should_include_page()` — —
- `extract_page_info()` — —
- `generate_encoder_test()` — —
- `generate_api_test()` — —
- `parse_makefile()` — —
- `parse_taskfile()` — —
- `parse_docker_compose()` — —
- `parse_buf_yaml()` — —
- `generate_testql_scenario()` — —
- `parse_router_prefix()` — —
- `parse_endpoints()` — —
- `collect_all_endpoints()` — —
- `collect_covered_endpoints()` — —
- `current_iteration()` — —
- `assign_wave()` — —
- `generate_scenario()` — —
- `extract_pages()` — —
- `ensure_dir()` — —
- `is_internal()` — —
- `crawl_routes()` — —
- `test_dashboard_url_navigation()` — —
- `test_files_url_navigation()` — —
- `test_url_structure()` — —
- `test_url_navigation()` — —
- `run_scenario()` — —
- `list_peripherals()` — —
- `test_iframe_pilot_toggle_smoke_off_on_and_child_shell_only()` — —
- `test_iframe_pilot_toggle_preserves_authenticated_session()` — —
- `test_service_id_pilot_guardrails_health_and_rollback_toggle()` — —
- `test_service_id_pilot_routing_toggle_smoke()` — —
- `test_service_id_session_continuity_and_cors_across_toggle()` — —
- `test_service_manager_pilot_routing_toggle_smoke()` — —
- `test_checked_in_menu_proto_artifacts_are_importable()` — —
- `test_menu_proto_contract_compiles()` — —
- `test_generated_menu_proto_modules_are_importable()` — —
- `test_normalize_icon_token_handles_svg_emoji_html_and_names()` — —
- `test_collect_changes_builds_filters_and_returns_only_changed_rows()` — —
- `test_collect_changes_supports_limit_and_offset()` — —
- `test_apply_changes_runs_update_per_row()` — —
- `test_apply_changes_limited_caps_updates()` — —
- `test_apply_changes_limited_zero_means_no_updates()` — —
- `page_file()` — —
- `test_resolve_protogate_python_prefers_explicit_path()` — —
- `test_resolve_protogate_python_uses_env_candidate()` — —
- `test_resolve_protogate_python_rejects_invalid_explicit()` — —
- `test_resolve_protogate_python_skips_non_executable_env_candidate()` — —
- `test_main_builds_expected_discovery_command()` — —
- `test_main_returns_error_when_explicit_python_invalid()` — —
- `test_parse_args_uses_canonical_default_output_dir()` — —
- `load_docs()` — —
- `ref_name()` — —
- `py_type()` — —
- `maybe_optional()` — —
- `with_default()` — —
- `render_anyof_validator()` — —
- `render_class()` — —
- `render_doc()` — —
- `render_module()` — —
- `render_init()` — —
- `ts_lit()` — —
- `ref_schema_name()` — —
- `zod_expr()` — —
- `render_defs()` — —
- `render_validators()` — —
- `validate_schema_file()` — —
- `collect_schema_files()` — —
- `add_ConnectDataCommandServiceServicer_to_server()` — —
- `add_ConnectDataQueryServiceServicer_to_server()` — —
- `add_ConnectIdCommandServiceServicer_to_server()` — —
- `add_ConnectIdQueryServiceServicer_to_server()` — —
- `add_ConnectScenarioCommandServiceServicer_to_server()` — —
- `add_ConnectScenarioQueryServiceServicer_to_server()` — —
- `add_ConnectTestCommandServiceServicer_to_server()` — —
- `add_ConnectTestQueryServiceServicer_to_server()` — —
- `add_ConnectTestProtocolCommandServiceServicer_to_server()` — —
- `add_ConnectTestProtocolQueryServiceServicer_to_server()` — —
- `register_logs_handlers()` — —
- `read_env_lines()` — —
- `parse_env()` — —
- `render_html_page()` — —
- `ui_root()` — —
- `save_env()` — —
- `init()` — —
- `walk()` — —
- `restore()` — —
- `report()` — —
- `dashboard()` — —
- `accelerator()` — —
- `serve()` — —
- `tui()` — —
- `version()` — —
- `duplicates()` — —
- `vector_build()` — —
- `vector_query()` — —
- `multi_repo()` — —
- `services()` — —
- `truth()` — —
- `plan()` — —
- `pr()` — —
- `execute()` — —
- `get_cc_for_day()` — —
- `generate_dashboard()` — —
- `launch_tui()` — —
- `test_run_aborts_when_baseline_snapshot_creation_fails()` — —
- `test_run_day_fast_stops_when_db_restore_fails()` — —
- `test_run_day_fast_stops_when_app_health_fails_after_db_restore()` — —
- `test_run_day_fast_uses_full_health_gate_after_db_restore()` — —
- `test_prewarm_worktrees_calls_get_or_create_for_every_sha()` — —
- `test_prewarm_worktrees_continues_after_individual_failure()` — —
- `test_needs_db_restore_returns_true_when_migration_file_changed()` — —
- `test_needs_db_restore_returns_false_when_no_db_files_changed()` — —
- `test_needs_db_restore_returns_true_when_no_previous_commit()` — —
- `test_needs_db_restore_returns_true_when_diff_fails()` — —
- `test_run_day_fast_skips_db_restore_when_no_db_files_changed()` — —
- `test_needs_rescan_returns_true_when_no_cache()` — —
- `test_needs_rescan_returns_true_when_route_file_changed()` — —
- `test_needs_rescan_returns_false_when_only_non_route_files_changed()` — —
- `test_needs_rescan_returns_true_when_diff_fails()` — —
- `test_run_day_fast_reuses_cached_endpoints_when_no_route_changes()` — —
- `test_diff_names_cached_calls_git_only_once_for_same_pair()` — —
- `test_diff_names_cached_returns_lowercased_paths()` — —
- `test_switch_commit_syncs_code_when_live_bind_swap_is_disabled()` — —
- `test_switch_commit_sets_current_sha_before_triggering_reload()` — —
- `test_prepare_runtime_creates_runtime_compose_and_active_link()` — —
- `test_accelerated_compose_up_uses_runtime_compose_override()` — —
- `test_switch_commit_uses_short_health_gate_even_with_long_config_timeout()` — —
- `test_switch_commit_skips_short_health_probe_when_recent_success_is_cached()` — —
- `test_switch_commit_rechecks_health_when_cache_is_stale()` — —
- `test_get_container_name_reuses_cached_container_lookup()` — —
- `test_get_container_name_caches_compose_ps_fallback_result()` — —
- `test_trigger_reload_caches_signal_strategy_after_first_success()` — —
- `test_sync_code_incremental_when_diff_is_small()` — —
- `test_sync_code_falls_back_to_full_copy_when_diff_exceeds_threshold()` — —
- `test_trigger_reload_caches_exec_strategy_after_signal_failure()` — —
- `test_start_dry_run_skips_deploy()` — —
- `test_start_none_method_returns_true()` — —
- `test_stop_dry_run_skips()` — —
- `test_stop_none_method_skips()` — —
- `test_start_none_returns_true()` — —
- `test_compose_file_finds_yml()` — —
- `test_compose_file_finds_yaml()` — —
- `test_compose_file_explicit()` — —
- `test_days_with_commits_parses_log()` — —
- `test_days_with_commits_filters_by_date_range()` — —
- `test_days_with_commits_empty_on_error()` — —
- `test_checkout_calls_git()` — —
- `test_restore_head_calls_git()` — —
- `test_restore_head_with_sha()` — —
- `test_clone_for_walk_creates_clone()` — —
- `test_execute_delegates_to_days_with_commits()` — —
- `test_load_history_empty_dir()` — —
- `test_load_history_no_results_json()` — —
- `test_load_history_single_day()` — —
- `test_load_history_multiple_days_sorted()` — —
- `test_load_history_health_pct()` — —
- `test_load_history_skips_invalid_dir_name()` — —
- `test_load_history_with_commit()` — —
- `test_load_history_status_timeout()` — —
- `test_load_history_testql_passed()` — —
- `test_endpoint_url()` — —
- `test_endpoint_url_strips_trailing_slash()` — —
- `test_endpoint_slug()` — —
- `test_day_result_health_pct_empty()` — —
- `test_day_result_health_pct()` — —
- `test_walk_config_defaults()` — —
- `test_run_returns_empty_when_no_commits()` — —
- `test_run_day_dry_run_skips_checkout()` — —
- `test_run_day_returns_day_result()` — —
- `test_run_day_deploy_failure_skips_scan()` — —
- `test_run_day_stop_always_called()` — —
- `test_run_processes_all_days()` — —
- `test_save_day_creates_results_file()` — —
- `test_save_day_creates_yaml_and_toon()` — —
- `test_save_day_includes_commit_in_json()` — —
- `test_save_day_no_output_dir_skips()` — —
- `test_save_day_health_in_json()` — —
- `test_save_html_creates_report_file()` — —
- `test_save_html_contains_health_pct()` — —
- `test_save_timeline_index_creates_index()` — —
- `test_save_timeline_index_multiple_days_sorted()` — —
- `test_find_last_working_day_found()` — —
- `test_find_last_working_day_picks_most_recent()` — —
- `test_find_last_working_day_none_when_always_fail()` — —
- `test_find_last_working_day_missing_dir()` — —
- `test_find_last_working_day_ignores_other_endpoints()` — —
- `test_find_last_working_day_skips_invalid_dirs()` — —
- `test_execute_returns_date()` — —
- `test_parse_openapi_returns_endpoints()` — —
- `test_parse_openapi_ignores_unknown_methods()` — —
- `test_parse_openapi_empty_paths()` — —
- `test_scan_via_compose_labels_finds_traefik_prefix()` — —
- `test_scan_via_compose_labels_no_compose_file()` — —
- `test_scan_via_compose_labels_dict_labels()` — —
- `test_ports_to_endpoints()` — —
- `test_ports_to_endpoints_no_services()` — —
- `test_execute_falls_back_to_health()` — —
- `test_execute_deta_takes_priority()` — —
- `test_execute_deduplicates_openapi_vs_deta()` — —
- `test_test_service_ok()` — —
- `test_test_service_timeout()` — —
- `test_test_service_set_day_dir()` — —
- `generate_readme()` — —
- `print()` — —
- `send_mvp_command()` — —
- `sendMVPCommand()` — —


## Project Structure

📄 `CHANGELOG`
📄 `Makefile`
📄 `PLAN`
📄 `README`
📄 `SUMD` (40890 functions)
📄 `SUMR`
📄 `TODO`
📄 `c2004.repo..testql.autoloop-state`
📄 `c2004.repo..testql.generated.connect-manager-test-types-fallback-api.testql.toon`
📄 `c2004.repo..testql.generated.connect-manager-test-types-gui-smoke-route2.testql.toon`
📄 `c2004.repo..testql.generated.connect-manager-test-types-gui-smoke.testql.toon`
📄 `c2004.repo..testql.generated.generated-api-smoke.testql.toon`
📄 `c2004.repo..testql.generated.generated-from-pytests.testql.toon`
📄 `c2004.repo..testql.generated.generated-from-scenarios.testql.toon`
📄 `c2004.repo..testql.generated.generated-hardware-smoke.testql.toon`
📄 `c2004.repo..testql.reports.iteration`
📄 `c2004.repo..testql.reports.llm-decision`
📄 `c2004.repo..testql.reports.pytest-iter1`
📄 `c2004.repo..testql.reports.pytest-iter4`
📄 `c2004.repo..testql.reports.pytest-iter5`
📄 `c2004.repo..testql.reports.pytest-iter6`
📄 `c2004.repo..testql.reports.pytest-iter7`
📄 `c2004.repo..testql.reports.stabilization-summary`
📄 `c2004.repo..testql.reports.testql-run-iter1`
📄 `c2004.repo..testql.reports.testql-run-iter4`
📄 `c2004.repo..testql.reports.testql-run-iter5`
📄 `c2004.repo..testql.reports.testql-run-iter7`
📄 `c2004.repo..testql.reports.topology`
📄 `c2004.repo..testql.reports.topology-iter6`
📄 `c2004.repo..testql.schemas.llm-decision.schema`
📄 `c2004.repo..testql.topology`
📄 `c2004.repo.testql-testing..testql.smoke-test.result`
📄 `docs.README` (1 functions)
📄 `docs.architecture`
📄 `docs.c2004`
📄 `docs.case_study_c2004`
📄 `docs.usage`
📄 `examples.01-dry-run-walk.README`
📄 `examples.01-dry-run-walk.run`
📄 `examples.02-docker-compose-project.README`
📄 `examples.02-docker-compose-project.docker-compose`
📄 `examples.02-docker-compose-project.run`
📄 `examples.03-restore-endpoint.README`
📄 `examples.03-restore-endpoint.mock_results`
📄 `examples.03-restore-endpoint.run`
📄 `examples.07-dsl-script.README`
📄 `examples.07-dsl-script.pipeline`
📄 `examples.08-nlp-commands.README` (4 functions)
📄 `examples.09-mvp-protocol.README` (5 functions)
📄 `examples.Makefile`
📄 `examples.restore_endpoint`
📄 `examples.walk_dry_run`
📄 `goal`
📄 `infra-map`
📄 `project`
📄 `project.README`
📄 `project.analysis.toon`
📄 `project.calls`
📄 `project.calls.toon`
📄 `project.context`
📄 `project.duplication.toon`
📄 `project.evolution.toon`
📄 `project.map.toon` (122859 functions)
📄 `project.project.toon`
📄 `project.prompt`
📄 `pyproject`
📄 `restored_c2004_health.api-health.README`
📄 `restored_c2004_health.api-health.backend.modules.connect-config-network.api.main` (9 functions)
📄 `restored_c2004_health.api-health.backend.modules.connect-id-user-list.api.main` (9 functions)
📄 `restored_c2004_health.api-health.backend.modules.connect-manager-library.api.main` (9 functions)
📄 `restored_c2004_health.api-health.backend.modules.connect-reports-month.api.main` (9 functions)
📄 `restored_c2004_health.api-health.backend.site.src.main` (52 functions)
📄 `restored_c2004_health.api-health.backend.site.vite.config`
📄 `restored_c2004_health.api-health.docker.Dockerfile`
📄 `restored_c2004_health.api-health.docker.docker-compose`
📄 `testql-scenarios.generated-cli-tests.testql.toon`
📄 `testql-scenarios.generated-from-pytests.testql.toon` (2 functions)
📄 `tree`

## Requirements

- Python >= >=3.11
- typer >=0.12- rich >=13- gitpython >=3.1- httpx >=0.27- pyyaml >=6- pydantic >=2- deta >=0.1- goal >=2.1.0- costs >=0.1.20- pfix >=0.1.60

## Contributing

**Contributors:**
- Tom Softreck <tom@sapletta.com>
- Tom Sapletta <tom-sapletta-com@users.noreply.github.com>

We welcome contributions! Open an issue or pull request to get started.
### Development Setup

```bash
# Clone the repository
git clone https://github.com/semcod/resplit
cd resplit

# Install in development mode
pip install -e ".[dev]"

# Run tests
pytest
```

## Documentation

- 🏛️ [Architecture](./docs/architecture.md) — Architecture with diagrams
- 💡 [Examples](./examples) — Usage examples and code samples

### Generated Files

| Output | Description | Link |
|--------|-------------|------|
| `README.md` | Project overview (this file) | — |
| `docs/architecture.md` | Architecture with diagrams | [View](./docs/architecture.md) |
| `examples` | Usage examples and code samples | [View](./examples) |

<!-- code2docs:end -->